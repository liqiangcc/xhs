1. JWT为什么是无状态的？相比session的优势。如果JWT被窃取，攻击者冒充登录怎么办（只答了黑名单机制）
2. 项目中怎么解决超卖
3. 为什么lua脚本能够保证原子性
4. 如果说并发量特别大，怎么应对？（Redis cluster）
5. redis cluster如何实现主从复制
6. 哨兵机制的实现，raft协议。
7. 场景题：服务器抖动怎么排查处理（答了cpu调度、垃圾回收）cms的全过程，哪里发生stw。
8. jvm怎么调整堆外内存大小。
9. jvm内存模型，方法区永久代元空间。
10. spring中bean的生命周期
11. 如果希望只有完全初始化好的bean投入使用（ApplicationContext.getBean()）
12. spring的事务传播机制（只答了required，nested，support，notsupport几个名词）
13. 线程池调度的全过程，为什么要有线程池，为什么线程切换有开销。
14. 怎么确定线程数。
15. MySQL的慢SQL调优。
16. 索引失效的几个情况。
17. 有索引的情况下，一条update语句执行很慢，为什么。
