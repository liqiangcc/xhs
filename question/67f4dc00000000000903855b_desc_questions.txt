1. 给定一个只包含数字的字符串 `s`，复原它并返回所有可能的 IP 地址格式。有效 IP 地址规则：由四个整数组成，每个整数位于 `0` 到 `255` 之间。整数不能含有前导 `0`（例如 `01` 不合法）。整数之间用 `'.'` 分隔。
2. 讲一下bi平台的业务流程
3. 限流的底层算法
4. 什么是令牌桶限流？
5. 多级缓存体系是怎么实现的？
6. 权限校验如何设计？（我回答的是使用Spring AOP实现了一个拦截器）
7. Spring AOP的底层是如何实现的？
8. Java并发编程介绍一下？
9. 并发编程的底层是如何实现的？
10. JVM提供的默认线程池都有什么？
