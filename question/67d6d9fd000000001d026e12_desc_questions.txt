1. 用户退出完了怎么返回数据给用户（存到数据库上，每次用户进去从数据库里拉）
2. 问SaaS是什么，消息队列用的是Kafka、RocketMQ还是RabbitMQ，写的消息队列是怎么实现的，把消息入队的并发处理又是怎么搞的。
3. 问中间件是啥
4. 问如果一个AIGC的结果半天无法返回，而用户又一定想要得到一个结果，那怎么办。
5. 算法题：把一个只包含1-9（可能不全有）的字符串分割成3个以上的数字，然后判断此字符串是否可以分割成斐波那契数列（如果是，必须是连着的。最后return bool）。
