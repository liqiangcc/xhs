1. MySQl的undolog和redo log 分别有什么⽤
2. MySQL慢查询什么原因) 怎么处理 Aexplain_
3. MySQL间隙锁是什么) 什么时候会用到
    1. 范围查询
    2. ⾮唯⼀索引
    3. 前向锁定
4. B+树相⽐B树的优势
5. Redis中有哪些数据类型) ZSet的底层原理知道吗
6. 跳表知道吗
7. Redis为什么快 A 漏了⼀个定制化的数据类型_
8. Redis部署有哪些形式) 怎么主从同步 APSYNC命令) 同步RDB⽂件) 以及环形复制积压缓冲
区) replId + Offset_
9. 讲⼀下布隆过滤器原理和使⽤场景
10. 双亲委派原理) 为了解决什么问题
    1. Bootstrap ClassLoader +Extension ClassLoader + Application ClassLoader
    2. 安全性+⼀致性+性能 A核⼼库只需要被加载⼀次_
11. CMS垃圾收集器的收集过程(初始标记+并发标记+重新标记+并发清除)
12. G1垃圾收集器会STW吗 A划分内存区域+可预测的STW时间+优先回收垃圾最⼤区域+标记复制
清除_
13. 守护线程通常具有较低的执⾏优先级) 因为它们的任务通常不是业务必需的｡
14. ⽣命周期, 守护线程的⽣命周期依赖于创建它的程序的⽣命周期｡ 当程序中所有⾮守护线程
结束时) 守护线程会⾃动结束｡
15. 资源回收和管理, 守护线程常⽤于后台任务) 如垃圾回收､ 内存管理等
16. TCP的TIME_WAIT和CLOSE_WAIT状态分别是什么
17. 算法题, 之字形打印⼆叉树
    1. 四次挥⼿结束后) 客户端TIME_WAIT > 2 * MSL
    2. 服务端收到FIN报⽂时) 响应⼀个 ACK A 确认_ 报⽂并进⼊ CLOSE_WAIT 状态) 等待本地
应⽤程序关闭
18. 反问, 位置, 清和园中⼼) 作息, 早10晚9
19. 拷打半⼩时项⽬, 架构是什么样⼦的) 为什么不⽤轮训⽽是MQ
