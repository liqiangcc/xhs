--- 图片 1 ---

滴滴 在线面试 一面
任务：单链表反转

// 默认模版
import java.io.*;
import java.util.*;

class Main {
    public static void main(String[] args) {
        Node node = new Node(1);
        Node pre = null, cur = node;
        while (cur != null) {
            Node tempNextNode = cur.next;
            cur.next = pre;
            pre = cur;
            cur = tempNextNode;
        }
    }
}

[图像摘要：滴滴在线面试界面的截图。左侧任务栏显示“单链表反转”。右侧代码编辑器中展示了 Java 实现单链表反转的逻辑片段。右上角显示面试计时 00:37:58。]
