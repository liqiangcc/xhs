# Answer Batch 0028 — Source-First Boundary Audit

- Evidence boundary: frozen `review/reports/ANSWER_BATCH_0028_SOURCE_PACKET.{json,md}` at extraction commit `b2e9ad771d5666c7742677b35a601fc91b947b5b` only. Raw caption/image text is authoritative over derived tagged wording, entities, validity flags, answer prose, or Canonical expansion.
- Audit rule: preserve every real recoverable interview Question, narrow unsupported derived wording, split source rows that contain multiple independent questions, and fail closed when a unique executable problem contract cannot be recovered. Historical answers, candidate prose, review scores, and remediation conclusions were not used to form the source dispositions below.
- Current-operation rule was checked only after source dispositions were formed: new Canonical relationships must use `canonical suggest -> dedup decide -> dedup apply`; existing Canonical maintenance uses the repository's supported merge/split path. No Question `canonical_id` may be edited by hand.
- Result: the 10 legacy Canonicals are **not** candidate-ready as a batch. Four are directly source-qualified, two require source-backed wording narrowing, one is source-unrecoverable and must fail closed, one merged source Question must be split into two independent Questions, one is a same-boundary duplicate candidate that needs explicit relation resolution, and one needs an explicit relation decision against a nearby deep-copy Concept Canonical before its Coding boundary is accepted.

| Canonical | Disposition | Source-first basis |
| --- | --- | --- |
| `cq_q_59d52495f3df25f0d98935d5e7fa3191` | candidate-qualified | Raw caption explicitly asks for SQL that returns IDs of people who read at least two articles on the same day, ordered by ID ascending. The task identity is recoverable. The source does **not** preserve concrete table/column names, so a candidate must state a minimal schema assumption instead of presenting invented names as interview facts. |
| `cq_q_5a5db0e8391add20113a1ffec9c1e41b` | normalize wording + candidate-qualified | Raw caption gives `1→2→3→3→4→4→5` and requires `1→2→5`. This uniquely means deleting **all nodes whose value is duplicated**, not the alternate variant that keeps one copy. The current `保留/去除重复节点` wording conflates two different linked-list contracts and must be narrowed before authoring. |
| `cq_q_5a87a04d4bc934eadb1cf42e28fcaed2` | normalize wording + candidate-qualified | Reliable raw caption asks how to store/check one million IPv4 addresses and specifically `ipv4转int`. It supports IPv4 string → 32-bit integer conversion, but not the current bidirectional `字符串与 int 之间的转换` claim. The packet's image-text artifact is an OCR/tool-failure transcript rather than interview evidence and must not be used. |
| `cq_q_5aa1b6ca0f00362ffc20d3cf8bc5f266` | candidate-qualified | Raw caption explicitly says `lc189 轮转数组` with `O(1)` space and `O(n)` time. The contract and complexity constraints are source-backed. The interviewer's later negative-modulo question is a follow-up, not an extra array-rotation input constraint. |
| `cq_q_5b53dba65e5ceb49b026dad8fc1704cc` | exclude — `incomplete_or_unreadable` | Strongest raw source contains only `数字拆分`. It preserves no input, output, objective, examples, constraints, or unique problem identity. `整数拆分` / `动态规划` are derived labels and cannot be promoted into a Coding contract. |
| `cq_q_5d39b5ae05a488c7436cbfa9b21e746c` | duplicate relation review required; do not author yet | Raw caption asks `如何翻转链表实现`. The already active source-qualified `cq_q_55c3a35aaf4f76ce9aab78ea39d9fddc` represents the same linked-list reversal operation. This is a newly detected same-boundary relationship and must go through `canonical suggest -> dedup decide -> dedup apply`; do not create a second curated answer or manually rebind the Question. |
| `cq_q_5e21e188af5c4a9ffdb5eaf97cc39c97` | explicit relation decision required before candidate authoring | Raw source in a frontend interview explicitly asks `如何实现一个深拷贝函数?`, so a JavaScript/TypeScript implementation response is recoverable, but the repository already has nearby `cq_deep_copy_5cfb8eb4` covering deep-copy semantics as a Concept asset. Coding implementation and Concept explanation are not automatically the same answer boundary. Record an explicit relation decision from current Canonical facts before deciding merge versus separate Canonicals; similarity alone is not authorization. |
| `cq_q_5f1aa586172b1a82ebb8cdd65fb6927b` | split source Question before authoring | Raw caption has two separate sections: **算法题** asks big-number addition from two strings, returning a string in `O(n)` with examples; **SQL题** asks, from student and grade tables, for student number, name and average grade. The current single Question/Canonical incorrectly fuses two independent interview questions and adds `JOIN/GROUP BY` as if source requirements. Split the source projection into two source-exact Questions and retire the mixed Canonical boundary before either answer is authored. |
| `cq_q_5f591ff5674d612dc10f87d07c1e820f` | candidate-qualified with minimal explicit API | Raw caption asks for binary-tree node storage-cost calculation and the maximum cost to reach a leaf, then follows up on the assumption behind computing the previous level before the next. This is sufficient to recover a weighted/root-to-leaf maximum-cost task at the operation level. The candidate must state its node-cost and return contract explicitly and must not invent edge-weight, balancing, or traversal constraints absent from source. |
| `cq_q_5f9a6152ed410f9a6b42f5f0ab7aa0a5` | candidate-qualified | Raw caption explicitly lists `手撕：节流`. A throttle-function implementation is recoverable from the frontend context. Leading/trailing-edge policy is not specified by source, so the candidate must state its chosen contract and distinguish common alternatives rather than claiming one policy was required. |

## Required remediation before candidate authoring

- Narrow `cq_q_5a5...` to the source-proven “delete every duplicated value from the sorted list” contract, and narrow `cq_q_5a87...` to source-proven IPv4 string → integer conversion. Rebuild Question/Canonical projections and generated indexes through the repository's bounded mutation path; do not hand-edit Canonical bindings.
- Retire `cq_q_5b53...` fail-closed. Preserve its raw source row as an explained invalid Question with `incomplete_or_unreadable`, remove active Canonical/Answer/ReviewProgress reachability as required by repository invariants, and keep an auditable archive rather than inventing an integer-partition problem.
- Resolve `cq_q_5d39...` against `cq_q_55c3...` through the current relation-candidate workflow with an explicit decision and fresh-state application. A second linked-list-reversal Answer is forbidden unless an explicit decision proves a materially different boundary.
- Route `cq_q_5e21...` and `cq_deep_copy_5cfb8eb4` through explicit relation review. The source-backed frontend implementation contract must not be silently absorbed into a Concept Canonical, but it also must not bypass duplicate review merely because its current type differs.
- Split the mixed source row behind `cq_q_5f1...` into two source-exact Questions using the repository's established guarded source-question split pattern: one for big-number string addition and one for the student/grade average SQL query. Retire the mixed Canonical/Answer/ReviewProgress state, create/reuse the resulting Canonicals through supported Application flows, and do not preserve derived `JOIN/GROUP BY` or other solution choices as source constraints.
- After every mutation, regenerate dependent projections/indexes and run the full integrity gates required by `AGENTS.md` and `docs/refactor/10_current_dedup_canonical_operations.md`. No answer may be promoted as part of boundary remediation.

## Guardrails for the next stage

- `cq_q_59d...`: SQL schema names are not in source; define assumptions and test duplicates/same-day grouping/order explicitly.
- `cq_q_5a5...`: implement the “remove all duplicated values” variant matching the source example; do not also answer the “keep one copy” problem as if it were the same contract.
- `cq_q_5a87...`: explain unsigned 32-bit value versus Java signed `int` representation; reverse int → IPv4 is an optional extension only, not a source requirement.
- `cq_q_5aa1...`: preserve the explicit `O(1)` extra-space / `O(n)` time requirements and handle `k` normalization without converting the follow-up about negative modulo into a hidden source constraint.
- `cq_q_5b53...`: no candidate, algorithm, DP recurrence, or LeetCode mapping may be invented from `数字拆分` alone.
- linked-list reversal: after relation resolution, exactly one active Canonical should own the same answer boundary and all original Questions must remain reachable through it.
- deep copy: if kept as a Coding Canonical, define the supported JavaScript value domain, cycles/shared references and unsupported host/resource values; factual implementation claims require primary-source evidence later. If merged, the formal decision must prove the merged Answer can satisfy both source response contracts and type gates.
- split big-number/SQL source: keep the two Questions independently reachable. Big-number addition has source-backed string inputs/output and `O(n)` time; the SQL question has source-backed tables/output fields but no source-prescribed join/aggregation syntax.
- `cq_q_5f591...`: state the node-cost/root-to-leaf model in the candidate's problem contract because the raw source does not preserve an API signature.
- `cq_q_5f9...`: state leading/trailing behavior and timer/time-source assumptions; do not present library-specific behavior as interviewer-specified.
