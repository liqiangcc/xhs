{
  "schema_version": "answer_scenario_validation.v1",
  "canonical_id": "cq_q_a791750e006147b75dcb2c13b7fa90e9",
  "validated_at": "2026-08-27",
  "command": "python3 hash_partition_validation.py",
  "result": "pass",
  "stdout": "PASS exact-intersection=2 duplicates-deduped same-url-same-bucket collision-does-not-match",
  "checks": [
    "identical URLs are routed to the same numbered partition on both sides",
    "the exact cross-file intersection contains the two expected URLs",
    "duplicate occurrences inside either input do not duplicate the unique-intersection output",
    "different URLs that share a partition are not reported equal because full-string equality is still required"
  ],
  "scope_note": "This deterministic fixture validates the partition-and-exact-compare invariant on bounded data. It does not claim a production memory, throughput, or disk-I/O result."
}
