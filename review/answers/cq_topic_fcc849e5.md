<!-- xhs-answer: {"schema_version":"answer.v1","canonical_id":"cq_topic_fcc849e5","version":2,"status":"ready","updated_at":"2026-07-10"} -->
# 微服务架构如何进行系统拆分？

## 核心结论

微服务拆分不是按技术层随便拆，而是按业务能力和领域边界拆。核心原则是高内聚、低耦合、数据自治、团队边界清晰，同时要控制分布式复杂度，避免过度拆分。

## 1 分钟版

我会先根据业务流程和领域模型识别核心子域，例如订单、支付、库存、用户、营销等，再看这些模块的数据归属、变更频率、团队职责和性能隔离需求。拆分后每个服务应该有清晰的业务语义和自己的数据边界，服务之间通过 API 或消息协作。对于强一致、强事务、频繁联表的能力，不会一开始就拆太细，而是先保持模块化，等边界稳定后再服务化。

## 3 分钟版

拆分步骤通常是：第一，从业务场景和主流程出发，梳理领域对象和业务能力；第二，按照 DDD 的限界上下文划分服务边界，让一个服务负责一类完整业务能力；第三，确认数据归属，避免多个服务直接写同一张核心表；第四，根据调用频率、性能瓶颈、发布频率、团队组织调整边界；第五，设计服务间通信和容错机制，包括幂等、重试、超时、降级、消息最终一致。拆分后还要用监控、链路追踪和契约测试保证可维护性。

## 关键细节

- 优先按业务能力拆，不按 Controller/Service/DAO 技术层拆。
- 服务要拥有自己的数据边界，避免共享数据库写入。
- 跨服务事务尽量转为最终一致、Saga 或可靠消息。
- 服务拆分会引入网络、部署、监控、治理成本。

## 原理机制

- 高内聚降低服务内部复杂度。
- 低耦合降低跨团队和跨服务变更成本。
- 康威定律说明系统边界会受到组织协作边界影响。
- DDD 限界上下文帮助定义模型语义边界。

## 项目经验版

项目映射可以电商领域推演订单、支付、库存、商品和用户边界，再检查数据所有权、调用频率与一致性链路。真实作答必须说明自己参与的边界决策、迁移步骤和指标；没有拆分经历时不要把示例写成个人项目。

## 常见追问

- 问：拆分后分布式事务怎么处理？答：先缩小强一致边界，再按业务选择本地事务加可靠消息、Saga/TCC 或补偿；不能把所有跨服务操作都包装成昂贵的全局事务。
- 问：服务之间循环依赖怎么办？答：重新检查领域边界和数据所有权，可抽取稳定能力、改为事件协作或建立防腐层；单纯增加调用层只会隐藏耦合。
- 问：如何判断服务拆得太细？答：如果一次业务请求跨越大量服务、发布必须联动、数据频繁跨服务事务且团队无法独立负责，说明边界可能过细或错误。
- 问：老单体如何渐进式拆分？答：先模块化和建立可观测性，再用绞杀者模式从边界清晰、收益高的能力切出，通过流量灰度和数据双写/校验逐步迁移。

## 易错点

- 不要只说“按模块拆”，要说明业务边界和数据边界。
- 不要忽略拆分后的治理成本。
- 不要把所有强一致场景都强行拆成微服务。
