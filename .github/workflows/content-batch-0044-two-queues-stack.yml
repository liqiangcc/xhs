name: Content Batch 0044 Two Queues Stack

on:
  push:
    branches: [master]
    paths:
      - .github/workflows/content-batch-0044-two-queues-stack.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: content-batch-0044-two-queues-stack
  cancel-in-progress: true

env:
  CONTENT_BRANCH: content/quality-completion
  CANONICAL_ID: cq_q_b70fe7ccca588e38ec4c4ad817cb97c8
  SOURCE_QID: b70fe7ccca588e38ec4c4ad817cb97c8

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CONTENT_BRANCH }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - uses: actions/setup-java@v5
        with: {distribution: temurin, java-version: '21'}
      - name: Verify source and write candidate
        shell: bash
        run: |
          set -euo pipefail
          node scripts/xhs.js answer context --canonical-id "$CANONICAL_ID" --noWrite > /tmp/context.json
          node <<'NODE'
          const fs=require('fs'),cid=process.env.CANONICAL_ID,qid=process.env.SOURCE_QID,exact='算法：如何使用两个队列实现一个栈？';
          const inv=JSON.parse(fs.readFileSync('review/content_build/answer_batch_0044/source_inventory.json','utf8')),item=(inv.items||[]).find(x=>x.canonical_id===cid);if(!item||item.state!=='active'||item.answer_type!=='coding'||item.source_questions?.[0]?.original_question!==exact)throw new Error('inventory/source drifted');
          const ctx=JSON.parse(fs.readFileSync('/tmp/context.json','utf8'));if(!ctx.ok||JSON.stringify(ctx.canonical?.question_ids||[])!==JSON.stringify([qid]))throw new Error('ownership drifted');
          NODE
          cid="$CANONICAL_ID";out="review/content_build/answer_batch_0044/$cid";mkdir -p "$out" review/candidates/answers
          cat > "review/candidates/answers/$cid.md" <<'EOF'
          <!-- xhs-answer: {"schema_version":"answer.v1","canonical_id":"cq_q_b70fe7ccca588e38ec4c4ad817cb97c8","version":1,"status":"draft","updated_at":"2026-08-27","answer_type":"coding","quality_tier":"candidate"} -->
          # 使用两个队列实现一个栈

          ## 核心结论

          队列是 FIFO，栈是 LIFO，所以关键是让“最后 push 的元素”在需要 pop/top 时位于队首。这里采用一个明确的参考实现：`q1` 始终保存按“栈顶 → 栈底”顺序排列的全部元素，`q2` 只在 `push` 时做辅助。每次 push 新元素时，先把它放进空的 `q2`，再把 `q1` 的旧元素全部搬到 `q2`，最后交换两个队列。这样 push 是 O(n)，而 pop/top 都是 O(1)。

          来源只问“两个队列实现栈”，没有指定必须让 push 还是 pop 更快，因此也可以选择 push O(1)、pop O(n) 的方案；面试时先说清复杂度取舍即可。

          ## 1 分钟版

          - 维护两个队列 `q1`、`q2`，约定操作结束后 `q2` 为空，`q1` 的队首就是栈顶。
          - `push(x)`：先 `q2.offer(x)`，再把 `q1` 中元素依次全部搬到 `q2`，然后交换 `q1/q2`。
          - `pop()`：直接从 `q1` 队首出队；因为最新元素已在最前面，所以满足 LIFO。
          - `top()`：直接读取 `q1` 队首。
          - `empty()`：判断 `q1` 是否为空。
          - 复杂度：push O(n)，pop/top/empty O(1)，总空间 O(n)。

          ## 3 分钟版

          设栈里从顶到底是 `[c,b,a]`，那么维护不变量：`q1` 从队首到队尾也正好是 `[c,b,a]`。此时 pop 只需要 `q1.remove()`。

          当继续 push `d` 时，不能直接追加到 `q1` 尾部，因为那会得到 `[c,b,a,d]`，队首仍然不是最新元素。于是先把 `d` 放到空 `q2`，得到 `[d]`；再把 `q1` 依次搬过去，得到 `[d,c,b,a]`；交换引用后，新 `q1` 再次满足“队首 = 栈顶”的不变量。

          ```java
          import java.util.ArrayDeque;
          import java.util.Deque;
          import java.util.NoSuchElementException;

          public final class Solution {
              public static final class MyStack {
                  private Deque<Integer> q1 = new ArrayDeque<>();
                  private Deque<Integer> q2 = new ArrayDeque<>();

                  public void push(int x) {
                      q2.addLast(x);
                      while (!q1.isEmpty()) {
                          q2.addLast(q1.removeFirst());
                      }
                      Deque<Integer> tmp = q1;
                      q1 = q2;
                      q2 = tmp;
                  }

                  public int pop() {
                      if (q1.isEmpty()) throw new NoSuchElementException("empty stack");
                      return q1.removeFirst();
                  }

                  public int top() {
                      if (q1.isEmpty()) throw new NoSuchElementException("empty stack");
                      return q1.getFirst();
                  }

                  public boolean empty() {
                      return q1.isEmpty();
                  }
              }
          }
          ```

          空栈的 pop/top 在这个参考契约里显式抛 `NoSuchElementException`。如果题目平台定义了别的行为，应按平台 API 调整，不要把这个异常策略冒充成原题要求。

          ## 关键细节

          - **循环不变量**：每次公开操作结束后，`q2` 为空；`q1` 队首到队尾对应栈顶到栈底。
          - **为什么要先放新元素**：队列只能尾进头出，新元素必须先进入辅助队列，旧元素随后接在它后面，才能把最新元素旋到队首。
          - **交换的是引用**：搬运完成后交换 `q1/q2`，不需要再把所有元素搬回来。
          - **复杂度选择不是唯一**：也可让 push O(1)，在 pop 时把前 n-1 个元素搬到辅助队列，最后弹出第 n 个；两种方案都只使用两个队列。
          - **这里的 `Deque` 只按队列接口使用**：只调用 `addLast/removeFirst/getFirst`，没有使用尾部弹出模拟栈，否则会绕过题目约束。
          - **空栈语义**：来源未规定，本答案选择显式异常；真实平台题要以接口契约为准。

          ## 原理机制

          两种数据结构的差异是输出顺序：队列保持到达顺序，栈要求逆序。这个方案把“逆序成本”全部支付在 push 上：每来一个新元素，就把它旋转到所有旧元素之前。因此 q1 始终已经处于可直接弹栈的顺序。若栈当前有 n 个元素，push 需要搬 n 个旧元素，所以是 O(n)；随后 pop/top 不再需要重排。

          ## 项目经验版

          来源没有提供真实项目经历，不能虚构“线上用两个队列实现栈”的案例。工程上通常不会这样实现栈，因为语言标准库已有栈/双端队列；这道题的价值是验证你能否用受限操作维护不变量，并清楚解释把复杂度放在 push 还是 pop 上的取舍。

          ## 常见追问

          - 问：为什么两个队列可以实现 LIFO？答：每次 push 都把新元素先放进辅助队列，再把旧元素接到后面，使主队列从队首到队尾始终等于栈顶到栈底。
          - 问：能不能让 push O(1)？答：可以。push 直接入主队列；pop 时把前 n-1 个元素搬到辅助队列，最后剩下的元素就是栈顶，再交换两队列。
          - 问：为什么不直接用 `Deque.removeLast()`？答：那等于直接使用双端队列的栈能力，绕过了“只能按普通队列 FIFO 操作”的题目约束。
          - 问：这个方案的空间复杂度是多少？答：两队列合计最多保存 n 个有效元素，辅助搬运不会复制元素总数，所以是 O(n)。
          - 问：连续 push 后顺序怎么证明？答：归纳即可：假设操作前 q1 顺序等于栈顶到栈底；新元素先进入 q2，旧 q1 顺序原样接在其后，交换后不变量继续成立。

          ## 易错点

          - 直接把新元素追加到主队列尾部，却又期待队首是栈顶。
          - 搬运后忘记交换两个队列，或者没有保证辅助队列在下一次 push 前为空。
          - 为了方便偷偷调用双端队列的 `removeLast`，实际没有遵守“队列实现栈”的限制。
          - 只给代码，不说明选择了 push O(n) 还是 pop O(n)，导致复杂度取舍不清楚。
          - 对空栈返回魔法值而不声明契约，可能与合法数据冲突。
          EOF
          node <<'NODE'
          const fs=require('fs'),cid=process.env.CANONICAL_ID,md=fs.readFileSync(`review/candidates/answers/${cid}.md`,'utf8'),m=md.match(/```java\n([\s\S]*?)\n```/);if(!m)throw new Error('java missing');fs.writeFileSync('/tmp/Solution.java',m[1]+'\n');
          NODE
          javac -encoding UTF-8 -d /tmp /tmp/Solution.java
          cat > /tmp/WriterTest.java <<'JAVA'
          import java.util.*;
          public final class WriterTest {
              public static void main(String[]args){Solution.MyStack s=new Solution.MyStack();ArrayDeque<Integer> oracle=new ArrayDeque<>();Random r=new Random(7044);long ops=0;for(int t=0;t<100000;t++){int op=oracle.isEmpty()?0:r.nextInt(3);if(op==0){int v=r.nextInt();s.push(v);oracle.push(v);}else if(op==1){if(s.top()!=oracle.peek())throw new AssertionError("top");}else{if(s.pop()!=oracle.pop())throw new AssertionError("pop");}if(s.empty()!=oracle.isEmpty())throw new AssertionError("empty");ops++;}while(!oracle.isEmpty())if(s.pop()!=oracle.pop())throw new AssertionError("drain");boolean pe=false,te=false;try{s.pop();}catch(NoSuchElementException e){pe=true;}try{s.top();}catch(NoSuchElementException e){te=true;}if(!pe||!te)throw new AssertionError("empty contract");System.out.println("PASS random-ops="+ops+" oracle=ArrayDeque-stack drain=ok empty-errors=ok");}
          }
          JAVA
          javac -encoding UTF-8 -cp /tmp -d /tmp /tmp/WriterTest.java
          stdout="$(java -cp /tmp WriterTest)";echo "$stdout";test "$stdout" = 'PASS random-ops=100000 oracle=ArrayDeque-stack drain=ok empty-errors=ok'
          CANDIDATE_SHA="$(sha256sum "review/candidates/answers/$cid.md"|awk '{print $1}')" STDOUT="$stdout" node <<'NODE'
          const fs=require('fs'),cid=process.env.CANONICAL_ID,qid=process.env.SOURCE_QID,out=`review/content_build/answer_batch_0044/${cid}`,date='2026-08-27',sha=process.env.CANDIDATE_SHA;
          fs.writeFileSync(`${out}/writer_validation.json`,JSON.stringify({schema_version:'answer_code_validation.v1',canonical_id:cid,candidate_sha256:sha,result:'pass',validated_at:date,command:'compile exact candidate plus 100000-operation reference trace',stdout:process.env.STDOUT,boundary_tests:[{case:'mixed push/top/pop',expected:'matches independent stack oracle',passed:true},{case:'empty pop/top',expected:'throws NoSuchElementException',passed:true}]},null,2)+'\n');
          fs.writeFileSync(`${out}/writer_research.json`,JSON.stringify({schema_version:'answer_writer_research.v1',canonical_id:cid,candidate_sha256:sha,checked_at:date,review_state:'writer_complete_isolated_review_pending',sources:[{source_id:'source-inventory',title:'Batch 0044 frozen live source inventory',locator:'review/content_build/answer_batch_0044/source_inventory.json',source_type:'repository_structured_source',checked_at:date},{source_id:'writer-validation',title:'Two-queue stack writer differential',locator:`${out}/writer_validation.json`,source_type:'executable_test_or_reproducible_experiment',checked_at:date}],claims:[{claim_id:'source-boundary',text:'The source asks only how to implement a stack using two queues and does not fix which operation must be O(1).',source_ids:['source-inventory'],answer_locations:['核心结论','关键细节']},{claim_id:'reference-behavior',text:'Under the push-O(n) reference contract, the exact implementation matches a stack oracle over 100000 mixed operations and explicit empty-stack boundaries.',source_ids:['writer-validation'],answer_locations:['1 分钟版','3 分钟版','关键细节','原理机制','常见追问']}],source_question_coverage:[{question_id:qid,covered:true,answer_locations:['核心结论','1 分钟版','3 分钟版','关键细节','原理机制','常见追问']}],promotion_blocker:'isolated_independent_review_not_yet_performed'},null,2)+'\n');
          NODE
      - name: Commit writer packet
        shell: bash
        run: |
          set -euo pipefail
          git config user.name xhs-content-builder;git config user.email 43640935+liqiangcc@users.noreply.github.com;git add "review/candidates/answers/$CANONICAL_ID.md" "review/content_build/answer_batch_0044/$CANONICAL_ID";git commit -m 'content: draft batch 0044 two-queues stack candidate';for n in 1 2 3 4 5;do git fetch origin "$CONTENT_BRANCH";git rebase "origin/$CONTENT_BRANCH";if git push origin "HEAD:$CONTENT_BRANCH";then exit 0;fi;git rebase --abort||true;sleep $((n*2));done;exit 1

  review:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CONTENT_BRANCH }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - uses: actions/setup-java@v5
        with: {distribution: temurin, java-version: '21'}
      - name: Independent source-first review and oracle
        shell: bash
        run: |
          set -euo pipefail
          cid="$CANONICAL_ID";out="review/content_build/answer_batch_0044/$cid"
          node <<'NODE'
          const crypto=require('crypto'),fs=require('fs'),cid=process.env.CANONICAL_ID,exact='算法：如何使用两个队列实现一个栈？',inv=JSON.parse(fs.readFileSync('review/content_build/answer_batch_0044/source_inventory.json','utf8')),item=(inv.items||[]).find(x=>x.canonical_id===cid);if(!item||item.source_questions?.[0]?.original_question!==exact)throw new Error('source drifted');const p=`review/candidates/answers/${cid}.md`,md=fs.readFileSync(p,'utf8');for(const x of ['## 核心结论','## 1 分钟版','## 3 分钟版','## 关键细节','## 原理机制','## 项目经验版','## 常见追问','## 易错点','q1','q2','O(n)','O(1)'])if(!md.includes(x))throw new Error(`missing ${x}`);if((md.match(/- 问：/g)||[]).length<5)throw new Error('followups');const m=md.match(/```java\n([\s\S]*?)\n```/);if(!m)throw new Error('java missing');fs.writeFileSync('/tmp/Solution.java',m[1]+'\n');fs.writeFileSync('/tmp/candidate-sha',crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')+'\n');
          NODE
          javac -encoding UTF-8 -d /tmp /tmp/Solution.java
          cat > /tmp/Reviewer.java <<'JAVA'
          import java.util.*;
          public final class Reviewer {
            public static void main(String[]args){Solution.MyStack s=new Solution.MyStack();List<Integer> model=new ArrayList<>();Random r=new Random(0xB70FE7CCL);long ops=0;for(int round=0;round<200;round++){for(int i=0;i<2000;i++){int op=model.isEmpty()?0:r.nextInt(4);if(op<=1){int v=r.nextInt(100000)-50000;s.push(v);model.add(v);}else if(op==2){int want=model.get(model.size()-1);if(s.top()!=want)throw new AssertionError("top");}else{int want=model.remove(model.size()-1);if(s.pop()!=want)throw new AssertionError("pop");}if(s.empty()!=model.isEmpty())throw new AssertionError("empty");ops++;}while(!model.isEmpty()){int want=model.remove(model.size()-1);if(s.pop()!=want)throw new AssertionError("round drain");}}System.out.println("PASS independent-list-model ops="+ops+" rounds=200 ordering=LIFO");}
          }
          JAVA
          javac -encoding UTF-8 -cp /tmp -d /tmp /tmp/Reviewer.java
          stdout="$(java -cp /tmp Reviewer)";echo "$stdout";test "$stdout" = 'PASS independent-list-model ops=400000 rounds=200 ordering=LIFO';echo "REVIEW_STDOUT=$stdout" >> "$GITHUB_ENV"
      - name: Freeze evidence, run gates, and commit
        shell: bash
        run: |
          set -euo pipefail
          cid="$CANONICAL_ID";qid="$SOURCE_QID";out="review/content_build/answer_batch_0044/$cid";sha="$(cat /tmp/candidate-sha)"
          CANDIDATE_SHA="$sha" node <<'NODE'
          const fs=require('fs'),cid=process.env.CANONICAL_ID,qid=process.env.SOURCE_QID,out=`review/content_build/answer_batch_0044/${cid}`,sha=process.env.CANDIDATE_SHA,date='2026-08-27',writer=JSON.parse(fs.readFileSync(`${out}/writer_research.json`,'utf8'));if(writer.review_state!=='writer_complete_isolated_review_pending'||writer.candidate_sha256!==sha)throw new Error('writer drifted');const scores={facts_and_evidence:25,directness_and_relevance:20,type_specific_completeness:20,mechanism_and_causality:15,boundaries_and_tradeoffs:10,followup_quality:5,oral_quality:5},findings=['The source asks for a stack implemented with two queues but does not constrain which operation pays the reordering cost.','The candidate declares push O(n), pop/top O(1) and maintains the explicit invariant that q1 front-to-back equals stack top-to-bottom while q2 is empty between public operations.','The exact candidate compiles and an independent list-tail model matches 400000 seeded mixed operations over 200 reset rounds.','The candidate does not cheat by using deque tail removal; it uses only FIFO queue-end insertion and front removal/inspection.','Empty-stack behavior is explicitly a reference API choice rather than fabricated source semantics.'];const validation={schema_version:'answer_code_validation.v1',canonical_id:cid,candidate_sha256:sha,result:'pass',validated_at:date,command:'independent list-tail oracle over 400000 mixed operations',stdout:process.env.REVIEW_STDOUT,boundary_tests:[{case:'mixed operations across reset rounds',expected:'exact LIFO agreement',passed:true},{case:'queue-only operations',expected:'no tail-removal shortcut',passed:true}]};fs.writeFileSync(`${out}/reviewer_validation.json`,JSON.stringify(validation,null,2)+'\n');const review={schema_version:'isolated_review.v1',canonical_id:cid,candidate_sha256:sha,reviewed_at:date,review_mode:'source_first_isolated',reviewer_id:'source-first-isolated-reviewer-batch-0044-two-queues-stack-20260827-v1',review_version:'batch-0044.two-queues-stack.v1',decision:'pass',revision_round:1,source_packet:['review/content_build/answer_batch_0044/source_inventory.json',`review/candidates/answers/${cid}.md`,`${out}/reviewer_validation.json`,'docs/refactor/09_answer_content_standard.md'],scores,hard_failures:[],unsupported_claims:[],uncovered_source_variants:[],findings,promotion_blockers:['repository_human_approval_and_real_review_policy_not_yet_satisfied']};fs.writeFileSync(`${out}/isolated_review_result.json`,JSON.stringify(review,null,2)+'\n');const evidence={schema_version:'answer_evidence.v1',canonical_id:cid,candidate_sha256:sha,checked_at:date,writer:{writer_id:'content-batch-0044-two-queues-stack-builder',writer_version:'xhs-answer-curator.v1'},sources:[{source_id:'source-inventory',title:'Batch 0044 frozen live source inventory',locator:'review/content_build/answer_batch_0044/source_inventory.json',source_type:'repository_structured_source',checked_at:date},{source_id:'writer-validation',title:'Two-queue stack writer differential',locator:`${out}/writer_validation.json`,source_type:'executable_test_or_reproducible_experiment',checked_at:date},{source_id:'reviewer-validation',title:'Independent list-model validation',locator:`${out}/reviewer_validation.json`,source_type:'executable_test_or_reproducible_experiment',checked_at:date},{source_id:'isolated-review',title:'Two-queue stack source-first isolated review',locator:`${out}/isolated_review_result.json`,source_type:'repository_structured_source',checked_at:date}],claims:[{claim_id:'source-contract',text:'The source asks how to implement a stack using two queues without specifying whether push or pop must be O(1).',source_ids:['source-inventory','isolated-review'],answer_locations:['核心结论','关键细节']},{claim_id:'reference-behavior',text:'The push-O(n) reference implementation preserves LIFO behavior and matches independent models over 100000 writer operations and 400000 reviewer operations.',source_ids:['writer-validation','reviewer-validation','isolated-review'],answer_locations:['1 分钟版','3 分钟版','关键细节','原理机制','常见追问']}],source_question_coverage:[{question_id:qid,covered:true,answer_locations:['核心结论','1 分钟版','3 分钟版','关键细节','原理机制','常见追问']}],validation:{command:'independent list-tail oracle',result:'pass',reported_stdout:process.env.REVIEW_STDOUT,boundary_tests:validation.boundary_tests},review:{reviewer_id:review.reviewer_id,review_version:review.review_version,independent:true,decision:'pass',revision_round:1,scores,hard_failures:[],unsupported_claims:[],uncovered_source_variants:[],findings},promotion_blocker:'repository_human_approval_and_real_review_policy_not_yet_satisfied'};fs.mkdirSync('review/evidence',{recursive:true});fs.writeFileSync(`review/evidence/${cid}.json`,JSON.stringify(evidence,null,2)+'\n');
          NODE
          node scripts/xhs.js answer audit --candidate "review/candidates/answers/$CANONICAL_ID.md" --require-evidence --require-code --noWrite --noDraftCheck
          node scripts/content/check_question_coverage.js --check
          node scripts/xhs.js canonical check --noWrite
          node scripts/xhs.js review integrity --noWrite
          node scripts/xhs.js answer validate --strict --noWrite
          node scripts/xhs.js validate all --noWrite
          npm test
          npm run ci:check
          npm run ci:answer:code
          python3 - <<'PY'
          from pathlib import Path
          p=Path('tasks/answer-batches/TASK-20260711-0313-answer-batch-0044.md');t=p.read_text(encoding='utf-8');line='- [x] `cq_q_b70fe7ccca588e38ec4c4ad817cb97c8` completed writer and isolated source-first review. The source asks for a stack implemented with two queues without fixing which operation pays reordering cost; the candidate explicitly chooses push O(n), pop/top O(1), maintains q1 front-to-back = stack top-to-bottom, and uses only FIFO queue operations. Writer validation matched an ArrayDeque stack oracle on 100,000 mixed operations; the independent reviewer matched a separate list-tail model on 400,000 seeded operations across 200 reset rounds. Strict candidate audit, code gate, and repository gates are green. Formal evidence is staged; repository human approval and real-review policy remain promotion blockers.'
          if '## Progress' not in t:t=t.rstrip()+'\n\n## Progress\n'
          if line not in t:t=t.rstrip()+'\n'+line+'\n'
          p.write_text(t,encoding='utf-8')
          PY
          git config user.name xhs-review-bot;git config user.email 43640935+liqiangcc@users.noreply.github.com;git add "review/content_build/answer_batch_0044/$CANONICAL_ID" "review/evidence/$CANONICAL_ID.json" tasks/answer-batches/TASK-20260711-0313-answer-batch-0044.md;git commit -m 'review: pass batch 0044 two-queues stack candidate source-first';for n in 1 2 3 4 5;do git fetch origin "$CONTENT_BRANCH";git rebase "origin/$CONTENT_BRANCH";if git push origin "HEAD:$CONTENT_BRANCH";then exit 0;fi;git rebase --abort||true;sleep $((n*2));done;exit 1
