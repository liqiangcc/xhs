name: Build Content Batch 0033 Final Two Candidates

on:
  push:
    branches: [master]
    paths:
      - .github/workflows/build-content-batch-0033-final-two-candidates.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: content-batch-0033-final-two-writer
  cancel-in-progress: true

env:
  CONTENT_BRANCH: content/quality-completion
  THROTTLE_CID: cq_q_7a75022f873e722084923cc7db60e1bb
  THROTTLE_QID: 7a75022f873e722084923cc7db60e1bb
  DEDUP_CID: cq_q_7b1c9751da7c787b856440f7bc4088c8
  DEDUP_QID: 7b1c9751da7c787b856440f7bc4088c8

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CONTENT_BRANCH }}
          fetch-depth: 1

      - id: base
        shell: bash
        run: echo "head=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
          cache: false

      - name: Freeze exact source boundaries and ownership
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs=require('fs');
          const cases=[
            {
              cid:process.env.THROTTLE_CID,qid:process.env.THROTTLE_QID,
              source:'note_tagged/689455070000000023019aa8.json',
              expected:'编程：单线程调用一个函数，实现一个限流器，当函数调用频率超过阈值时返回false，否则True。',
              boundary:[
                'Source-preserved facts: this is a single-thread coding task; repeated calls are admitted or rejected by returning true/false according to a rate threshold.',
                'Not preserved by the source: fixed vs sliding window, whether rejected attempts count, the threshold/window units, clock API, exact boundary semantics, language/API, invalid-clock behavior, and concurrency semantics beyond the explicit single-thread condition.',
                'Candidate policy: implement an admission sliding-window limiter in Java with an injected non-negative non-decreasing millisecond clock; permit at most maxCalls accepted calls in the half-open interval (now-windowMillis, now]; rejected attempts do not consume capacity; exact-boundary accepted timestamps expire.'
              ]
            },
            {
              cid:process.env.DEDUP_CID,qid:process.env.DEDUP_QID,
              source:'note_tagged/67e12cfa000000001d02777a.json',
              expected:'算法：删除链表所有重复数字（不允许用map或者set）',
              boundary:[
                'Source-preserved facts: this is a linked-list coding task asking to delete duplicate numbers and explicitly forbidding Map/Set in the solution.',
                'Not preserved by the source: whether the list is sorted, whether duplicate means keep one copy or remove every occurrence of any repeated value, node/API shape, mutation policy, value range, language, and cycle behavior.',
                'Candidate policy: do not assume sorting; interpret “删除所有重复数字” as removing every node whose value occurs more than once anywhere in the original acyclic list; use no Map/Set in the solution, relink in place, preserve the relative order of values that occur exactly once, and return the new head.'
              ]
            }
          ];
          const rows=fs.readFileSync('data/questions/questions.jsonl','utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
          const canons=fs.readFileSync('data/questions/canonical_questions.jsonl','utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse);
          const boundaryPath='review/content_build/answer_batch_0033/source_boundary_audit.md';
          let boundary=fs.readFileSync(boundaryPath,'utf8');
          for(const c of cases){
            const tagged=JSON.parse(fs.readFileSync(c.source,'utf8'));
            const source=(tagged.tagged_questions||[]).find(x=>x.question_id===c.qid);
            if(!source||source.original_question!==c.expected) throw new Error(`${c.qid}: source wording drifted: ${source?.original_question}`);
            if(source.question_type!=='算法手撕_Coding') throw new Error(`${c.qid}: source type drifted: ${source.question_type}`);
            const row=rows.find(x=>x.question_id===c.qid);
            if(!row||row.canonical_id!==c.cid) throw new Error(`${c.qid}: Question -> Canonical ownership drifted: ${row?.canonical_id}`);
            const canon=canons.find(x=>x.canonical_id===c.cid);
            if(!canon||!(canon.question_ids||[]).includes(c.qid)) throw new Error(`${c.cid}: Canonical ownership drifted: ${JSON.stringify(canon?.question_ids)}`);
            if(fs.existsSync(`review/candidates/answers/${c.cid}.md`)) throw new Error(`${c.cid}: candidate already exists; refuse to overwrite writer artifact`);
            const heading=`## ${c.cid}`;
            if(!boundary.includes(heading)){
              boundary += `\n${heading}\n\n- Exact repository source: \`${c.source}\`.\n- Preserved wording: \`${c.expected}\`\n- ${c.boundary[0]}\n- ${c.boundary[1]}\n- ${c.boundary[2]}\n- Promotion remains blocked until isolated independent review verifies source boundaries, exact fenced-code behavior, evidence, and repository gates.\n`;
            }
          }
          fs.writeFileSync(boundaryPath,boundary);
          NODE

      - name: Write sliding-window rate-limiter candidate and fixture
        shell: bash
        run: |
          set -euo pipefail
          cid="$THROTTLE_CID"; out="review/content_build/answer_batch_0033/$cid"; mkdir -p "$out"
          cat > "review/candidates/answers/$cid.md" <<'EOF'
          <!-- xhs-answer: {"schema_version":"answer.v1","canonical_id":"cq_q_7a75022f873e722084923cc7db60e1bb","version":1,"status":"draft","updated_at":"2026-08-26","answer_type":"coding","quality_tier":"candidate"} -->
          # 单线程函数限流器：超过频率阈值返回 false

          ## 核心结论

          来源只说明“单线程调用一个函数，实现限流器，调用频率超过阈值时返回 false，否则 true”，没有规定窗口算法和时间边界。这里明确采用**滑动时间窗口的准入语义**：在任意调用时刻 `now`，只统计区间 `(now - windowMillis, now]` 内已经返回 `true` 的调用；若其中已有 `maxCalls` 次则本次返回 `false`，并且被拒绝的调用不写入窗口。单线程条件下用时间戳队列即可，不需要锁。

          ## 1 分钟版

          - 构造参数是 `maxCalls`、`windowMillis` 和可注入时钟；本答案要求时钟返回非负、单调不减的毫秒值，这是为了让测试可重复，也避免系统时间回拨把窗口语义搞乱。
          - 每次 `allow()` 先删除所有 `timestamp <= now - windowMillis` 的已放行记录，因此恰好位于左边界的调用已经过期。
          - 清理后如果队列大小已经达到 `maxCalls`，返回 `false`；拒绝记录不入队，所以持续重试不会主动延长封禁时间。
          - 否则把当前时间入队并返回 `true`。队列只保存当前窗口内的成功准入，长度最多是 `maxCalls`。
          - 单次最坏可能清理多个旧时间戳，但每个时间戳只入队和出队一次，所以均摊时间 O(1)，额外空间 O(maxCalls)。

          ## 3 分钟版

          ```java
          import java.util.ArrayDeque;
          import java.util.Deque;
          import java.util.Objects;
          import java.util.function.LongSupplier;

          public final class SlidingWindowRateLimiter {
              private final int maxCalls;
              private final long windowMillis;
              private final LongSupplier clock;
              private final Deque<Long> accepted = new ArrayDeque<>();
              private long lastNow = -1L;

              public SlidingWindowRateLimiter(int maxCalls, long windowMillis, LongSupplier clock) {
                  if (maxCalls <= 0) throw new IllegalArgumentException("maxCalls must be > 0");
                  if (windowMillis <= 0) throw new IllegalArgumentException("windowMillis must be > 0");
                  this.maxCalls = maxCalls;
                  this.windowMillis = windowMillis;
                  this.clock = Objects.requireNonNull(clock, "clock");
              }

              public boolean allow() {
                  long now = clock.getAsLong();
                  if (now < 0) throw new IllegalStateException("clock must be non-negative");
                  if (lastNow >= 0 && now < lastNow) {
                      throw new IllegalStateException("clock must be non-decreasing");
                  }
                  lastNow = now;

                  long cutoff = now >= windowMillis ? now - windowMillis : -1L;
                  while (!accepted.isEmpty() && accepted.peekFirst() <= cutoff) {
                      accepted.removeFirst();
                  }
                  if (accepted.size() >= maxCalls) {
                      return false;
                  }
                  accepted.addLast(now);
                  return true;
              }
          }
          ```

          例如 `maxCalls=3, windowMillis=1000`，在 `0/100/200ms` 三次调用都返回 `true`，`300ms` 返回 `false`。到 `1000ms` 时，`0ms` 恰好落在左边界并被淘汰，所以 `1000ms` 可以再次返回 `true`。如果 `999ms` 的拒绝请求也写入队列，就会改变恢复时间；本答案明确选择“只统计已放行调用”的准入契约。

          ## 关键细节

          - **阈值到底统计什么**：来源只说“调用频率”，没有说明是所有尝试还是成功准入。本实现统计成功准入；若要对原始尝试频率限流，应在判断前记录每次尝试，行为会不同。
          - **窗口边界**：本契约使用 `(now-windowMillis, now]`。因此 `now-windowMillis` 上的旧记录过期，避免一个时间戳同时属于相邻两个完整窗口边界。
          - **为什么不用固定窗口计数器**：固定窗口只需一个计数器和窗口起点，但边界两侧可以出现瞬时双倍突发；滑动窗口更贴合“任意连续时间段最多 N 次”的契约，代价是保存最多 N 个时间戳。
          - **为什么队列长度有界**：达到 `maxCalls` 后后续请求直接拒绝且不入队；清理只会缩短队列，所以 `accepted.size() <= maxCalls` 始终成立。
          - **时钟**：示例不直接调用 `System.currentTimeMillis()`，而是注入 `LongSupplier`，这样边界测试无需 sleep。真实实现可注入单调时间源适配器；如果使用会回拨的墙上时钟，应定义回拨策略。
          - **单线程边界**：来源明确是单线程，所以 `ArrayDeque` 和普通字段足够。若多个线程并发调用，清理、容量判断、入队必须作为一个原子临界区处理，不能直接把这段代码宣称为线程安全。
          - **复杂度**：每个成功时间戳入队一次、最多出队一次，因此 m 次调用的队列操作总量 O(m)，均摊 O(1)；空间 O(maxCalls)。

          ## 原理机制

          这个限流器维护一个不变量：`accepted` 中只包含当前滑动窗口内、已经成功放行的调用时间戳，并按时间非降序排列。调用开始先推进左边界，把不再属于窗口的旧时间戳移除；此时队列大小就是当前窗口已经消耗的配额。若大小等于上限，拒绝而不改变状态；否则追加 `now`，让本次调用占用一个配额。因为输入时间单调不减，队首永远是最先过期的记录，所以只需要 FIFO 队列。

          固定窗口、令牌桶和漏桶都可以满足不同的“限流”目标，但状态语义不同。固定窗口强调每个离散窗口的计数；令牌桶允许按桶容量吸收突发；漏桶更接近稳定输出速率。来源没有指定这些语义，因此不能把其中任何一个说成原题唯一答案。

          ## 项目经验版

          来源没有真实线上 QPS、租户维度或故障数据，不能虚构生产经验。实际落地前需要确认阈值维度（全局、用户、接口还是资源）、允许突发程度、拒绝是否计数、时间源、分布式一致性和失败策略。单机单线程队列算法只证明核心状态机；跨进程限流还要选择共享状态或分片策略，并单独验证延迟与一致性代价。

          ## 常见追问

          - 问：为什么拒绝请求不入队？答：这是本答案明确选择的“成功准入消耗配额”契约。若业务要限制所有尝试频率，应把每次尝试都记录，恢复时间会被高频重试推迟。
          - 问：为什么 `1000ms` 时 `0ms` 的记录要过期？答：因为窗口定义为 `(now-1000, now]`，左端开区间；边界规则必须先定义，否则测试会出现 off-by-one 争议。
          - 问：固定窗口为什么可能突发翻倍？答：如果上一个窗口末尾放行 N 次、下一个窗口开头又放行 N 次，那么很短的连续区间里可能观察到接近 2N 次成功调用。
          - 问：多线程还能直接用吗？答：不能。队列清理、大小判断和追加之间存在竞态；要加锁、使用原子状态机或换成并发安全设计。
          - 问：如果只要求平均速率并允许短时突发呢？答：令牌桶通常更自然：按速率补充令牌、桶容量定义最大突发；这和当前“任意滑动窗口最多 N 次”不是同一个契约。

          ## 易错点

          - 没定义“超过阈值”统计成功准入还是所有调用尝试，就直接写代码。
          - 窗口左右边界不明确，导致恰好在 `windowMillis` 处多算或少算一次。
          - 拒绝请求也写入成功队列，却仍声称“拒绝不消耗配额”。
          - 用墙上时钟但不处理时间回拨，队列顺序和过期判断失效。
          - 来源明确单线程，却反过来把无锁 `ArrayDeque` 实现宣传成并发线程安全。
          EOF

          CANDIDATE="review/candidates/answers/$cid.md" OUT="$out" node <<'NODE'
          const fs=require('fs'); const text=fs.readFileSync(process.env.CANDIDATE,'utf8');
          const open='```java\n',i=text.indexOf(open); if(i<0) throw new Error('Java fence missing');
          const start=i+open.length,end=text.indexOf('\n```',start); if(end<0) throw new Error('Java fence close missing');
          fs.writeFileSync(`${process.env.OUT}/SlidingWindowRateLimiter.java`,text.slice(start,end)+'\n');
          NODE
          cat > "$out/SlidingWindowRateLimiterTest.java" <<'JAVA'
          import java.util.function.LongSupplier;

          public final class SlidingWindowRateLimiterTest {
              static final class FakeClock implements LongSupplier {
                  long now;
                  FakeClock(long initial) { now = initial; }
                  void set(long value) { now = value; }
                  @Override public long getAsLong() { return now; }
              }
              static void expect(boolean actual, boolean expected, String name) {
                  if (actual != expected) throw new AssertionError(name + ": expected=" + expected + " actual=" + actual);
              }
              static void expectThrows(Runnable r, String name) {
                  try { r.run(); } catch (RuntimeException expected) { return; }
                  throw new AssertionError(name + ": expected exception");
              }
              public static void main(String[] args) {
                  FakeClock c = new FakeClock(0);
                  SlidingWindowRateLimiter limiter = new SlidingWindowRateLimiter(3, 1000, c);
                  expect(limiter.allow(), true, "t0");
                  c.set(100); expect(limiter.allow(), true, "t100");
                  c.set(200); expect(limiter.allow(), true, "t200");
                  c.set(300); expect(limiter.allow(), false, "t300-over-limit");
                  c.set(999); expect(limiter.allow(), false, "t999-rejected-attempt-not-counted");
                  c.set(1000); expect(limiter.allow(), true, "exact-boundary-expires-t0");
                  c.set(1001); expect(limiter.allow(), false, "still-three-accepted");
                  c.set(1100); expect(limiter.allow(), true, "exact-boundary-expires-t100");

                  FakeClock burstClock = new FakeClock(42);
                  SlidingWindowRateLimiter burst = new SlidingWindowRateLimiter(2, 10, burstClock);
                  expect(burst.allow(), true, "burst-1");
                  expect(burst.allow(), true, "burst-2");
                  expect(burst.allow(), false, "burst-3");
                  burstClock.set(52); expect(burst.allow(), true, "burst-expired-at-boundary");

                  FakeClock reg = new FakeClock(5);
                  SlidingWindowRateLimiter regLimiter = new SlidingWindowRateLimiter(1, 10, reg);
                  expect(regLimiter.allow(), true, "reg-first");
                  reg.set(4); expectThrows(regLimiter::allow, "clock-regression");
                  expectThrows(() -> new SlidingWindowRateLimiter(0, 1, () -> 0L), "bad-limit");
                  expectThrows(() -> new SlidingWindowRateLimiter(1, 0, () -> 0L), "bad-window");
                  SlidingWindowRateLimiter neg = new SlidingWindowRateLimiter(1, 1, () -> -1L);
                  expectThrows(neg::allow, "negative-clock");
                  System.out.println("PASS sliding-window accepted-only exact-boundary burst regression invalid-config");
              }
          }
          JAVA
          javac "$out/SlidingWindowRateLimiter.java" "$out/SlidingWindowRateLimiterTest.java"
          stdout="$(java -cp "$out" SlidingWindowRateLimiterTest)"
          test "$stdout" = 'PASS sliding-window accepted-only exact-boundary burst regression invalid-config'
          STDOUT="$stdout" node <<'NODE'
          const fs=require('fs'),path=require('path');
          const cid=process.env.THROTTLE_CID,qid=process.env.THROTTLE_QID,out=`review/content_build/answer_batch_0033/${cid}`;
          fs.writeFileSync(path.join(out,'code_validation.json'),JSON.stringify({schema_version:'answer_code_validation.v1',canonical_id:cid,result:'pass',validated_at:'2026-08-26',command:'javac SlidingWindowRateLimiter.java SlidingWindowRateLimiterTest.java && java SlidingWindowRateLimiterTest',stdout:process.env.STDOUT,checks:['three accepted calls then rejection inside same sliding window','rejected attempts do not consume capacity','exact left boundary expiry','same-timestamp burst limit','clock regression rejected','invalid configuration rejected'],exact_fenced_code_matches_fixture:true},null,2)+'\n');
          fs.writeFileSync(path.join(out,'writer_research.json'),JSON.stringify({schema_version:'answer_writer_research.v1',canonical_id:cid,checked_at:'2026-08-26',review_state:'writer_complete_isolated_review_pending',sources:[{source_id:'repository-source',title:'Exact tagged interview question',locator:'note_tagged/689455070000000023019aa8.json',source_type:'repository_source_record',checked_at:'2026-08-26'},{source_id:'fixture',title:'Deterministic JDK 21 sliding-window limiter fixture',locator:`${out}/code_validation.json`,source_type:'executable_test_or_reproducible_experiment',checked_at:'2026-08-26'}],claims:[{claim_id:'source-boundary',text:'The source preserves only a single-thread true/false rate-limiter coding task; window algorithm, attempt-vs-admission counting, clock/boundary/API semantics are not preserved.',source_ids:['repository-source'],answer_locations:['核心结论','关键细节','易错点']},{claim_id:'algorithm',text:'Under the explicit accepted-only sliding-window contract, an ordered timestamp deque evicts expired admissions, rejects at capacity, and otherwise appends now.',source_ids:['fixture'],answer_locations:['1 分钟版','3 分钟版','原理机制']},{claim_id:'validation',text:'The exact fenced Java passes deterministic limit, rejection, exact-boundary, burst, clock-regression and invalid-configuration cases.',source_ids:['fixture'],answer_locations:['3 分钟版','关键细节','常见追问']}],source_question_coverage:[{question_id:qid,covered:true,answer_locations:['核心结论','1 分钟版','3 分钟版','关键细节','原理机制','常见追问']}],promotion_blocker:'isolated_independent_review_not_yet_performed'},null,2)+'\n');
          NODE

      - name: Write unsorted linked-list duplicate-removal candidate and fixture
        shell: bash
        run: |
          set -euo pipefail
          cid="$DEDUP_CID"; out="review/content_build/answer_batch_0033/$cid"; mkdir -p "$out"
          cat > "review/candidates/answers/$cid.md" <<'EOF'
          <!-- xhs-answer: {"schema_version":"answer.v1","canonical_id":"cq_q_7b1c9751da7c787b856440f7bc4088c8","version":1,"status":"draft","updated_at":"2026-08-26","answer_type":"coding","quality_tier":"candidate"} -->
          # 删除链表所有重复数字：不使用 Map / Set

          ## 核心结论

          来源只说“删除链表所有重复数字（不允许用 map 或 set）”，没有说明链表已排序，也没有说明“删除重复”是保留一份还是把重复值全部删除。这里采用更明确的主契约：**链表可以无序；只要某个值在原链表出现超过一次，就删除这个值的所有节点；值只出现一次的节点按原顺序保留；不使用 Map/Set，原地修改 `next` 并返回新头结点。** 在不能用哈希集合且不假设排序时，可以对每个当前值向后扫描并删除所有同值节点，发现过重复后再删除当前节点，时间 O(n²)、额外空间 O(1)。

          ## 1 分钟版

          - 先用 dummy 头结点统一“删除原头结点”和“删除中间结点”的写法；`prev` 永远指向当前节点前一个已经确认保留的节点。
          - 对 `current` 的值，用 `runner` 扫描它后面的链表；遇到同值节点就立即摘掉，并把 `duplicated=true`。
          - 如果扫描结束发现过同值节点，说明 `current.val` 在原链表至少出现两次：后面的重复已经删掉，再用 `prev.next=current.next` 删除当前节点。
          - 如果没有发现重复，当前值只出现一次，保留它并让 `prev/current` 同步前进。
          - 外层最多 n 次、每次向后扫描 O(n)，所以最坏 O(n²)；只用固定数量指针和一个布尔值，额外空间 O(1)，solution 中没有 Map/Set。

          ## 3 分钟版

          ```java
          public final class DeleteAllDuplicateValues {
              public static final class ListNode {
                  public int val;
                  public ListNode next;
                  public ListNode(int val) { this.val = val; }
              }

              public static ListNode deleteAllDuplicates(ListNode head) {
                  ListNode dummy = new ListNode(0);
                  dummy.next = head;
                  ListNode prev = dummy;
                  ListNode current = head;

                  while (current != null) {
                      boolean duplicated = false;
                      ListNode runnerPrev = current;
                      ListNode runner = current.next;
                      while (runner != null) {
                          if (runner.val == current.val) {
                              duplicated = true;
                              runnerPrev.next = runner.next;
                              runner = runnerPrev.next;
                          } else {
                              runnerPrev = runner;
                              runner = runner.next;
                          }
                      }

                      if (duplicated) {
                          prev.next = current.next;
                          current = prev.next;
                      } else {
                          prev = current;
                          current = current.next;
                      }
                  }
                  return dummy.next;
              }
          }
          ```

          例如 `1 -> 2 -> 3 -> 2 -> 4 -> 1 -> 5 -> 5 -> 6`，值 `1/2/5` 都出现超过一次，因此这些值的所有节点都被删掉，结果是 `3 -> 4 -> 6`。算法不依赖相同值相邻，所以不需要“链表已排序”这个来源没有提供的假设。

          ## 关键细节

          - **语义先澄清**：本答案把“所有重复数字”解释成“重复值整组删除”。如果面试官想表达的是“去重但保留一个”，那么发现后续同值节点时只删除后续节点、不要再删除 `current` 即可。
          - **不假设有序**：若题目明确链表已排序，重复值必然连续，可以用单次线性扫描做到 O(n)；当前来源没有排序条件，所以不能把 LeetCode 有序链表前提偷偷带进来。
          - **为什么只向后找就够**：如果一个值在后面还有相同节点，那么第一次处理该值时就会把所有后续同值节点和当前节点一起删除；因此能够进入后续外层循环的已保留前缀，每个值都已经证明不会在剩余后缀再次出现。
          - **指针安全**：删除 `runner` 时不能让 `runnerPrev` 前进，因为它仍然是新的 `runner` 的前驱；只有当前 `runner` 被保留时两个游标才一起向前。
          - **dummy 的作用**：当原头值本身重复时，`prev=dummy` 让删除逻辑和中间位置完全一致，无需为 head 单独分支。
          - **空间限制**：solution 没有 Map/Set，也没有把值复制到数组排序；测试代码可以使用独立频次 oracle 验证实现，但那不是提交给题目的算法。
          - **输入边界**：空链表、单节点、全唯一、全重复、头尾重复和非相邻重复都需要覆盖。本答案假设输入是普通无环单链表；若可能有环，应先定义是否拒绝或先断环。

          ## 原理机制

          核心不变量是：进入每轮外层循环时，`dummy.next ... prev` 这一段只包含最终应该保留的唯一值，`current` 是尚未判定的第一个节点。内层扫描会消除 `current.val` 在后缀中的所有其他出现，并记录它是否重复。若重复，当前节点也必须删掉，已确认前缀不变；若不重复，`current` 可以安全加入已确认前缀。这个不变量让算法即使修改链表结构，也不会漏掉后续非相邻重复。

          如果允许 O(n) 额外空间，最直接的通用无序方案是先统计每个值的频次，再第二遍只保留频次为 1 的节点；但题目显式禁止 Map/Set，所以这里用时间换空间。若输入已排序，则可以利用“重复连续”这个额外结构，把 O(n²) 降为 O(n)。

          ## 项目经验版

          来源没有真实业务链表或性能约束，不能虚构项目经历。面试现场应先问两件事：链表是否排序，以及“删除重复”到底是保留一份还是重复值全部删除。这两个条件会直接决定最优算法。如果 n 很大且链表无序，同时又严格禁止额外集合，O(n²) 是需要明确说出的时间代价；不能只强调 O(1) 空间而忽略性能。

          ## 常见追问

          - 问：为什么不用快慢指针一次做完？答：无序链表里同值节点可能相隔很远；没有额外集合就无法只看局部状态判断某个值是否还会再次出现，所以一般需要重复扫描。
          - 问：如果链表已经排序呢？答：相同值连续，可以用 dummy + 双指针按重复段扫描，一次 O(n) 删除整段重复值。
          - 问：如果“重复”只是想保留第一个呢？答：内层发现同值时删除后续重复即可，处理完后保留 `current`；那是另一份输出契约。
          - 问：为什么测试 oracle 可以用 Map？答：限制针对解题实现。测试需要一个独立、容易证明正确的参考实现来验证 solution；只要正式解法不依赖 Map/Set，就没有绕过题目约束。
          - 问：算法会改变输入链表吗？答：会，本契约原地重连 `next`。如果要求输入不可变，就必须新建节点，空间语义也会改变。

          ## 易错点

          - 默认链表有序，写 O(n) 连续段算法，却没有任何来源依据。
          - 把“删除所有重复值”和“去重保留一个”混成同一个问题。
          - 内层删除节点后仍然推进 `runnerPrev`，导致跳过连续待检查节点。
          - 删除当前头结点时没有 dummy，产生大量 head 特判或断链。
          - 宣称 O(1) 额外空间却偷偷把所有值复制到数组、Map 或 Set。
          EOF

          CANDIDATE="review/candidates/answers/$cid.md" OUT="$out" node <<'NODE'
          const fs=require('fs'); const text=fs.readFileSync(process.env.CANDIDATE,'utf8');
          const open='```java\n',i=text.indexOf(open); if(i<0) throw new Error('Java fence missing');
          const start=i+open.length,end=text.indexOf('\n```',start); if(end<0) throw new Error('Java fence close missing');
          fs.writeFileSync(`${process.env.OUT}/DeleteAllDuplicateValues.java`,text.slice(start,end)+'\n');
          NODE
          cat > "$out/DeleteAllDuplicateValuesTest.java" <<'JAVA'
          import java.util.ArrayList;
          import java.util.Arrays;
          import java.util.List;
          import java.util.Map;
          import java.util.TreeMap;

          public final class DeleteAllDuplicateValuesTest {
              static DeleteAllDuplicateValues.ListNode list(int... values) {
                  DeleteAllDuplicateValues.ListNode dummy = new DeleteAllDuplicateValues.ListNode(0), tail = dummy;
                  for (int v : values) { tail.next = new DeleteAllDuplicateValues.ListNode(v); tail = tail.next; }
                  return dummy.next;
              }
              static int[] array(DeleteAllDuplicateValues.ListNode head) {
                  List<Integer> out = new ArrayList<>();
                  for (var p = head; p != null; p = p.next) out.add(p.val);
                  return out.stream().mapToInt(Integer::intValue).toArray();
              }
              static int[] oracle(int[] input) {
                  Map<Integer,Integer> counts = new TreeMap<>();
                  for (int v : input) counts.merge(v, 1, Integer::sum);
                  return Arrays.stream(input).filter(v -> counts.get(v) == 1).toArray();
              }
              static void check(int[] in, int[] expected) {
                  int[] actual = array(DeleteAllDuplicateValues.deleteAllDuplicates(list(in)));
                  if (!Arrays.equals(actual, expected)) throw new AssertionError(Arrays.toString(in)+" -> "+Arrays.toString(actual)+" expected "+Arrays.toString(expected));
              }
              public static void main(String[] args) {
                  check(new int[]{}, new int[]{});
                  check(new int[]{1}, new int[]{1});
                  check(new int[]{1,2,3}, new int[]{1,2,3});
                  check(new int[]{1,1}, new int[]{});
                  check(new int[]{1,2,1,3}, new int[]{2,3});
                  check(new int[]{1,2,2,3,3,4}, new int[]{1,4});
                  check(new int[]{1,2,3,2,4,1,5,5,6}, new int[]{3,4,6});
                  int exhaustive = 0;
                  for (int len = 0; len <= 7; len++) {
                      int total = 1;
                      for (int i=0;i<len;i++) total *= 4;
                      for (int mask=0; mask<total; mask++) {
                          int[] in = new int[len]; int x=mask;
                          for (int i=0;i<len;i++) { in[i]=x%4; x/=4; }
                          check(in, oracle(in)); exhaustive++;
                      }
                  }
                  System.out.println("PASS fixed=7 exhaustive="+exhaustive+" unsorted=true no-map-set-in-solution=true");
              }
          }
          JAVA
          javac "$out/DeleteAllDuplicateValues.java" "$out/DeleteAllDuplicateValuesTest.java"
          stdout="$(java -cp "$out" DeleteAllDuplicateValuesTest)"
          test "$stdout" = 'PASS fixed=7 exhaustive=21845 unsorted=true no-map-set-in-solution=true'
          STDOUT="$stdout" node <<'NODE'
          const fs=require('fs'),path=require('path');
          const cid=process.env.DEDUP_CID,qid=process.env.DEDUP_QID,out=`review/content_build/answer_batch_0033/${cid}`;
          fs.writeFileSync(path.join(out,'code_validation.json'),JSON.stringify({schema_version:'answer_code_validation.v1',canonical_id:cid,result:'pass',validated_at:'2026-08-26',command:'javac DeleteAllDuplicateValues.java DeleteAllDuplicateValuesTest.java && java DeleteAllDuplicateValuesTest',stdout:process.env.STDOUT,checks:['empty and single-node lists','all-unique list','all-duplicate pair','non-adjacent duplicate values','multiple duplicate groups','head/middle/tail duplicate groups','all 21,845 arrays of lengths 0..7 over values 0..3 against independent frequency oracle'],exact_fenced_code_matches_fixture:true},null,2)+'\n');
          fs.writeFileSync(path.join(out,'writer_research.json'),JSON.stringify({schema_version:'answer_writer_research.v1',canonical_id:cid,checked_at:'2026-08-26',review_state:'writer_complete_isolated_review_pending',sources:[{source_id:'repository-source',title:'Exact tagged interview question',locator:'note_tagged/67e12cfa000000001d02777a.json',source_type:'repository_source_record',checked_at:'2026-08-26'},{source_id:'fixture',title:'Deterministic JDK 21 unsorted linked-list duplicate-removal fixture',locator:`${out}/code_validation.json`,source_type:'executable_test_or_reproducible_experiment',checked_at:'2026-08-26'}],claims:[{claim_id:'source-boundary',text:'The source asks to delete duplicate numbers from a linked list without Map/Set but does not preserve sortedness or keep-one-vs-remove-all semantics.',source_ids:['repository-source'],answer_locations:['核心结论','关键细节','易错点']},{claim_id:'algorithm',text:'Under the explicit unsorted/remove-all repeated-values contract, forward scanning removes later equal nodes and then removes current when any duplicate is found, using constant auxiliary pointers.',source_ids:['fixture'],answer_locations:['1 分钟版','3 分钟版','原理机制']},{claim_id:'validation',text:'The exact fenced Java passes named boundaries and all 21,845 arrays of lengths 0..7 over four values against an independent frequency oracle.',source_ids:['fixture'],answer_locations:['3 分钟版','关键细节','常见追问']}],source_question_coverage:[{question_id:qid,covered:true,answer_locations:['核心结论','1 分钟版','3 分钟版','关键细节','原理机制','常见追问']}],promotion_blocker:'isolated_independent_review_not_yet_performed'},null,2)+'\n');
          NODE

      - name: Record writer slices
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs=require('fs');
          const task='tasks/answer-batches/TASK-20260711-0313-answer-batch-0033.md';
          let text=fs.readFileSync(task,'utf8');
          const marker='- [ ] Every candidate passes `answer audit --require-evidence`.';
          const notes=[
            '- [ ] `cq_q_7a75022f873e722084923cc7db60e1bb` writer slice staged source-first: single-thread true/false limiter wording is preserved; sliding-window, accepted-only counting, injected clock and exact-boundary semantics remain explicit answer-side contracts; exact fenced Java passes deterministic capacity, rejection, boundary, burst and clock/config fixtures. Isolated independent review is still required before promotion.',
            '- [ ] `cq_q_7b1c9751da7c787b856440f7bc4088c8` writer slice staged source-first: no sorting or keep-one semantics are invented; the candidate explicitly chooses unsorted/remove-all-repeated-values semantics with no Map/Set in the solution; exact fenced Java passes named cases plus all 21,845 bounded arrays against an independent frequency oracle. Isolated independent review is still required before promotion.'
          ];
          if(!text.includes(marker)) throw new Error('batch 0033 task marker missing');
          for(const note of notes) if(!text.includes(note)) text=text.replace(marker,marker+'\n'+note);
          fs.writeFileSync(task,text);
          NODE

      - name: Audit writer candidates and repository gates
        shell: bash
        run: |
          set -euo pipefail
          node scripts/xhs.js answer audit --candidate "review/candidates/answers/$THROTTLE_CID.md" --require-code --noWrite --noManifest
          node scripts/xhs.js answer audit --candidate "review/candidates/answers/$DEDUP_CID.md" --require-code --noWrite --noManifest
          node scripts/content/check_question_coverage.js --check
          node scripts/xhs.js canonical check --noWrite
          node scripts/xhs.js review integrity --noWrite
          node scripts/xhs.js answer type-audit --check --noWrite
          node scripts/xhs.js answer validate --strict --noWrite
          node scripts/xhs.js validate all --noWrite
          npm test
          npm run ci:check
          npm run ci:answer:semantic
          npm run ci:answer:code
          npm run ci:answer:coverage

      - name: Commit writer slice only from stable content head
        shell: bash
        run: |
          set -euo pipefail
          git fetch --depth=1 origin "$CONTENT_BRANCH"
          remote_head="$(git rev-parse origin/$CONTENT_BRANCH)"
          base_head='${{ steps.base.outputs.head }}'
          if [ "$remote_head" != "$base_head" ]; then
            echo "Content branch advanced during writer slice: $base_head -> $remote_head; fail closed and retry later."
            exit 0
          fi
          git add \
            "review/candidates/answers/$THROTTLE_CID.md" \
            "review/candidates/answers/$DEDUP_CID.md" \
            "review/content_build/answer_batch_0033/source_boundary_audit.md" \
            "review/content_build/answer_batch_0033/$THROTTLE_CID" \
            "review/content_build/answer_batch_0033/$DEDUP_CID" \
            tasks/answer-batches/TASK-20260711-0313-answer-batch-0033.md
          git diff --cached --check
          git config user.name 'xhs-content-builder'
          git config user.email '43640935+liqiangcc@users.noreply.github.com'
          git commit -m 'content: stage final two batch 0033 candidates source-first'
          git push origin "HEAD:$CONTENT_BRANCH"
