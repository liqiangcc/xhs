name: Remediate Batch 0031 B+ Tree Java Class Boundary

on:
  push:
    branches: [master]
    paths:
      - .github/workflows/remediate-batch-0031-bplus-tree-java-class.yml
  workflow_dispatch:

permissions:
  contents: write

env:
  CONTENT_BRANCH: content/quality-completion
  CANONICAL_ID: cq_q_6eac192f81a68eadb5b67c48d493f791
  OLD_BLOB_SHA: ba7552dd3b6b1ad3d3bd267948753cbaab5db338

jobs:
  remediate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CONTENT_BRANCH }}
          fetch-depth: 1
      - id: base
        shell: bash
        run: echo "head=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - name: Replace method-only snippet with standalone compilable class
        shell: bash
        run: |
          set -euo pipefail
          candidate="review/candidates/answers/$CANONICAL_ID.md"
          test "$(git hash-object "$candidate")" = "$OLD_BLOB_SHA"
          node - <<'NODE'
          const fs=require('fs'),cid=process.env.CANONICAL_ID;
          const p=`review/candidates/answers/${cid}.md`;
          let text=fs.readFileSync(p,'utf8');
          const old=`\`\`\`java
public static int maxFanout(long usableInternalBytes, long keyBytes, long childPointerBytes) {
    return (int) ((usableInternalBytes + keyBytes) / (keyBytes + childPointerBytes));
}

public static int leafCapacity(long usableLeafBytes, long leafEntryBytes) {
    return (int) (usableLeafBytes / leafEntryBytes);
}

public static int minimumFullLevels(long records, int fanout, int leafCapacity) {
    if (records <= leafCapacity) return 1;
    long capacity = leafCapacity;
    int levels = 1;
    while (capacity < records) {
        capacity = Math.multiplyExact(capacity, (long) fanout);
        levels++;
    }
    return levels;
}
\`\`\``;
          const replacement=`\`\`\`java
public final class BPlusTreeSizing {
    private BPlusTreeSizing() {}

    public static int maxFanout(long usableInternalBytes, long keyBytes, long childPointerBytes) {
        requirePositive(usableInternalBytes, "usableInternalBytes");
        requirePositive(keyBytes, "keyBytes");
        requirePositive(childPointerBytes, "childPointerBytes");
        long fanout = (usableInternalBytes + keyBytes) / (keyBytes + childPointerBytes);
        if (fanout < 2 || fanout > Integer.MAX_VALUE) {
            throw new IllegalArgumentException("fanout outside supported range");
        }
        return (int) fanout;
    }

    public static int leafCapacity(long usableLeafBytes, long leafEntryBytes) {
        requirePositive(usableLeafBytes, "usableLeafBytes");
        requirePositive(leafEntryBytes, "leafEntryBytes");
        long capacity = usableLeafBytes / leafEntryBytes;
        if (capacity < 1 || capacity > Integer.MAX_VALUE) {
            throw new IllegalArgumentException("leaf capacity outside supported range");
        }
        return (int) capacity;
    }

    public static int minimumFullLevels(long records, int fanout, int leafCapacity) {
        if (records < 0) throw new IllegalArgumentException("records must be non-negative");
        if (fanout < 2) throw new IllegalArgumentException("fanout must be at least 2");
        if (leafCapacity < 1) throw new IllegalArgumentException("leafCapacity must be positive");
        if (records <= leafCapacity) return 1;
        long capacity = leafCapacity;
        int levels = 1;
        while (capacity < records) {
            if (capacity > Long.MAX_VALUE / fanout) return levels + 1;
            capacity *= fanout;
            levels++;
        }
        return levels;
    }

    private static void requirePositive(long value, String name) {
        if (value <= 0) throw new IllegalArgumentException(name + " must be positive");
    }
}
\`\`\``;
          if(!text.includes(old)) throw new Error('method-only Java snippet not found');
          text=text.replace(old,replacement);
          fs.writeFileSync(p,text);
          NODE
          out="review/content_build/answer_batch_0031/$CANONICAL_ID"
          javac "$out/BPlusTreeSizing.java" "$out/BPlusTreeSizingTest.java"
          test "$(java -cp "$out" BPlusTreeSizingTest)" = 'PASS fixed=7 fanout-oracle=2000 height-oracle=5000 model=f-child-pointers-plus-f-minus-1-keys validation=yes'

      - name: Commit only from stable content head
        shell: bash
        run: |
          set -euo pipefail
          git fetch --depth=1 origin "$CONTENT_BRANCH"
          test "$(git rev-parse origin/$CONTENT_BRANCH)" = '${{ steps.base.outputs.head }}'
          git add "review/candidates/answers/$CANONICAL_ID.md"
          git diff --cached --check
          git config user.name 'xhs-content-builder'
          git config user.email '43640935+liqiangcc@users.noreply.github.com'
          git commit -m 'content: make B+ tree sizing Java snippet auditable'
          git push origin "HEAD:$CONTENT_BRANCH"
