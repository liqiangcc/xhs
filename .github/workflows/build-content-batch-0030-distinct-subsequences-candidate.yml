name: Build Content Batch 0030 Distinct Subsequences Candidate

on:
  push:
    branches: [master]
    paths:
      - .github/workflows/build-content-batch-0030-distinct-subsequences-candidate.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: content-batch-0030-distinct-subsequences-candidate
  cancel-in-progress: true

env:
  CONTENT_BRANCH: content/quality-completion
  CANONICAL_ID: cq_q_6c3e7f9d826622bce0c25df5beee24a0
  SOURCE_QID: 6c3e7f9d826622bce0c25df5beee24a0

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CONTENT_BRANCH }}
          fetch-depth: 1

      - id: base
        shell: bash
        run: echo "head=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
          cache: false

      - name: Guard LeetCode 115 source boundary and Canonical ownership
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs=require('fs'),cid=process.env.CANONICAL_ID,qid=process.env.SOURCE_QID;
          const expected='算法：不同的子序列 (LeetCode 115 - DP)';
          const tagged=JSON.parse(fs.readFileSync('note_tagged/6661b50a0000000006006ab3.json','utf8'));
          const tq=(tagged.tagged_questions||[]).find(x=>x.question_id===qid);
          if(!tq||tq.original_question!==expected||tq.question_type!=='算法手撕_Coding'||!(tq.tech_entities||[]).includes('动态规划')) throw new Error('tagged LeetCode 115 source boundary drifted');
          const structured=JSON.parse(fs.readFileSync('note_structured/6661b50a0000000006006ab3.json','utf8'));
          if(!(structured.questions||[]).includes(expected)) throw new Error('structured LeetCode 115 source wording drifted');
          const q=fs.readFileSync('data/questions/questions.jsonl','utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse).find(x=>x.question_id===qid);
          if(!q||q.original_question!==expected||q.canonical_id!==cid) throw new Error(`Question projection drifted: ${JSON.stringify(q)}`);
          const canon=fs.readFileSync('data/questions/canonical_questions.jsonl','utf8').split(/\r?\n/).filter(Boolean).map(JSON.parse).find(x=>x.canonical_id===cid);
          if(!canon||JSON.stringify(canon.question_ids||[])!==JSON.stringify([qid])) throw new Error(`Canonical ownership drifted: ${JSON.stringify(canon?.question_ids)}`);
          NODE
          node scripts/xhs.js answer context --canonical-id "$CANONICAL_ID" --noWrite > /tmp/distinct-subseq-context.json
          node <<'NODE'
          const fs=require('fs'),ctx=JSON.parse(fs.readFileSync('/tmp/distinct-subseq-context.json','utf8'));
          if(!ctx.ok||ctx.canonical?.canonical_id!==process.env.CANONICAL_ID||ctx.answer_type!=='coding') throw new Error('AnswerContext drifted');
          NODE

      - name: Write source-bounded LeetCode 115 candidate and exhaustive fixture
        shell: bash
        run: |
          set -euo pipefail
          cid="$CANONICAL_ID"; out="review/content_build/answer_batch_0030/$cid"; candidate="review/candidates/answers/$cid.md"
          mkdir -p "$out" review/candidates/answers
          test ! -e "$candidate"

          cat > "$candidate" <<'EOF'
          <!-- xhs-answer: {"schema_version":"answer.v1","canonical_id":"cq_q_6c3e7f9d826622bce0c25df5beee24a0","version":1,"status":"draft","updated_at":"2026-08-25","answer_type":"coding","quality_tier":"candidate"} -->
          # LeetCode 115：不同的子序列——计数型字符串 DP

          ## 核心结论

          仓库来源明确保留了“LeetCode 115 - DP”，因此题意可以回到官方问题契约核对：给定字符串 `s` 和 `t`，统计 `s` 的多少个不同**下标选择方案**形成的子序列恰好等于 `t`；官方测试保证最终答案落在 32 位有符号整数范围内。这里的“不同”是选择了不同的源下标，不是把得到的字符串值去重。

          最直接的状态是 `dp[i][j] = s[0..i) 形成 t[0..j) 的方案数`。如果 `s[i-1] != t[j-1]`，当前字符不能用，只能继承 `dp[i-1][j]`；如果相等，可以不用当前字符，也可以把它作为 `t[j-1]`，所以 `dp[i][j] = dp[i-1][j] + dp[i-1][j-1]`。空间压缩成一维后，`j` 必须从右往左更新，否则同一个 `s[i]` 会在本轮被重复使用。

          ## 1 分钟版

          - 定义 `dp[j]`：扫描到当前 `s` 前缀后，形成 `t` 前 `j` 个字符的方案数。
          - `dp[0] = 1`：任何 `s` 前缀形成空串都有一种方法——什么都不选。
          - 扫描一个 `s[i]` 时，如果它等于 `t[j-1]`，新增方案就是此前形成 `t[0..j-1)` 的所有方案，因此 `dp[j] += dp[j-1]`。
          - `j` 必须**倒序**。正序会先修改 `dp[j-1]`，再拿这个“已经用过当前字符”的新值去更新 `dp[j]`，等价于让同一个源字符被选两次。
          - 时间复杂度 O(|s|·|t|)，空间 O(|t|)。如果 `|t| > |s|` 可以直接返回 0。
          - 官方只保证最终答案是 32 位整数；为避免未证明的中间计数溢出，下面实现把中间值安全饱和到 `Integer.MAX_VALUE + 1`，最后在官方契约下再转成 `int`。

          ## 3 分钟版

          ```java
          public final class Solution {
              private static final long LIMIT = (long) Integer.MAX_VALUE + 1;

              public int numDistinct(String s, String t) {
                  if (s == null || t == null) {
                      throw new IllegalArgumentException("s/t must not be null");
                  }
                  if (t.length() > s.length()) return 0;

                  long[] dp = new long[t.length() + 1];
                  dp[0] = 1;

                  for (int i = 0; i < s.length(); i++) {
                      int upper = Math.min(t.length(), i + 1);
                      for (int j = upper; j >= 1; j--) {
                          if (s.charAt(i) == t.charAt(j - 1)) {
                              dp[j] = Math.min(LIMIT, dp[j] + dp[j - 1]);
                          }
                      }
                  }

                  if (dp[t.length()] > Integer.MAX_VALUE) {
                      throw new ArithmeticException("result exceeds official 32-bit contract");
                  }
                  return (int) dp[t.length()];
              }
          }
          ```

          例如 `s="rabbbit"`、`t="rabbit"` 的答案是 3；`s="babgbag"`、`t="bag"` 的答案是 5。每一条计数都对应一组严格递增的 `s` 下标。

          ## 关键细节

          - **状态含义一定要说清楚**：统计的是“下标选择方案”，所以源字符串里值相同的两个字符如果位置不同，选择它们形成的是不同方案。
          - **为什么 `dp[0]=1`**：空目标只有“不选任何字符”这一种形成方式。这也是递推中匹配第一个目标字符时 `dp[1] += dp[0]` 的来源。
          - **为什么倒序**：二维转移只允许从上一行 `i-1` 读取。倒序更新一维数组时，`dp[j-1]` 尚未被当前 `s[i]` 改写，因此仍等价于上一行；正序会破坏这个不变量。
          - **为什么可以饱和而不影响官方合法答案**：所有转移都是非负计数的“保留或相加”，没有减法。某个中间状态一旦真实值超过 `Integer.MAX_VALUE`，以后只会保持或增加。如果这个超大状态后来对最终状态有正贡献，最终答案也必然超过官方承诺；若它永远不贡献，保留其精确巨大值也没有意义。因此把中间值截到 `MAX+1` 能防止 `long` 也被组合数撑爆，同时仍精确得到所有官方合法的最终答案。
          - **`t` 比 `s` 长**：不可能通过删除字符让序列变长，直接返回 0。
          - **空串**：官方约束要求 `s/t` 长度至少 1；实现仍自然支持空目标返回 1、空源对非空目标返回 0，这是 DP 边界的泛化，不应反过来冒充官方输入约束。
          - **复杂度**：双循环最多处理 |s|×|t| 个状态；一维数组长度 |t|+1，所以 O(|s|·|t|) 时间、O(|t|) 额外空间。

          ## 原理机制

          对于二维状态 `F(i,j)`，考虑源前缀最后一个字符 `s[i-1]`。任何形成 `t[0..j)` 的合法选择，要么**不选**这个字符，于是方案已经包含在 `F(i-1,j)`；要么**选**它作为目标最后一个字符，这只有在 `s[i-1] == t[j-1]` 时才合法，并且前面必须从 `s[0..i-1)` 形成 `t[0..j-1)`，共有 `F(i-1,j-1)` 种。这两个集合按“是否选择下标 i-1”互斥且覆盖全部方案，因此相加不会重不漏。

          一维压缩不是改变递推，而只是复用二维表的一行内存。倒序让右侧状态继续读取“上一行”的左邻居；可以把它理解成保持一个数据依赖拓扑。如果写成正序，`dp[j-1]` 已经包含当前字符带来的新方案，当前字符就会穿过同一行再次参与 `dp[j]`，违反子序列每个源下标最多选择一次的定义。

          ## 项目经验版

          来源没有真实项目背景，不能虚构“线上用过这道 DP”。如果这类计数逻辑进入业务，我会先冻结“不同”的身份语义（按位置、按值还是按对象 ID）、结果上界和溢出策略，再用一个小规模穷举 oracle 对随机/全量小输入交叉验证优化后的 DP；只有状态定义和计数身份一致，性能优化才有意义。

          ## 常见追问

          - 问：为什么匹配时不是只写 `dp[j] = dp[j-1]`？答：因为当前字符可以选，也可以不选；不选的旧 `dp[j]` 方案仍然有效，所以要把两部分相加。
          - 问：为什么正序一定错？答：正序时更新 `dp[j]` 读到的 `dp[j-1]` 可能已经使用了当前 `s[i]`，于是同一个源下标会在一次扫描里贡献多次；倒序保证读到的是上一轮状态。
          - 问：和最长公共子序列有什么区别？答：LCS 优化的是长度，状态转移取 `max`；本题固定目标 `t`，统计所有形成它的下标方案，转移做计数相加。
          - 问：能不能用递归 + 记忆化？答：可以，状态仍是 `(i,j)`，每个状态最多计算一次，渐进复杂度也是 O(|s|·|t|)；迭代 DP 更容易做一维空间压缩且没有深递归栈。
          - 问：官方说最终答案不超过 int，为什么还不直接用 int？答：最终状态有上界，不等于所有目标前缀的中间组合数都已经被证明有相同上界。饱和计数只多用 `long`，可以把这个未证明的中间溢出风险显式关掉。
          - 问：什么时候可以提前返回 0？答：最直接的是 `t.length() > s.length()`；还可以做字符频次等剪枝，但不会改变最坏 O(|s|·|t|) 的核心 DP，也不是本题必要条件。

          ## 易错点

          - 把“不同子序列”理解成对最终字符串内容去重，而不是不同下标选择。
          - 一维 DP 从左往右更新，导致一个源字符在同一轮被重复使用。
          - 忘记 `dp[0]=1`，于是第一个目标字符永远无法获得计数。
          - 匹配时覆盖旧 `dp[j]`，丢掉“不使用当前字符”的方案。
          - 只因为官方最终答案 fits int，就未经证明地宣称所有中间前缀计数也不会溢出。
          - 把实现支持空串的泛化边界错误说成官方题目约束。
          EOF

          cat > "$out/DistinctSubsequences115.java" <<'JAVA'
          public final class DistinctSubsequences115 {
              private static final long LIMIT = (long) Integer.MAX_VALUE + 1;

              public static int numDistinct(String s, String t) {
                  if (s == null || t == null) throw new IllegalArgumentException("s/t must not be null");
                  if (t.length() > s.length()) return 0;
                  long[] dp = new long[t.length() + 1];
                  dp[0] = 1;
                  for (int i = 0; i < s.length(); i++) {
                      int upper = Math.min(t.length(), i + 1);
                      for (int j = upper; j >= 1; j--) {
                          if (s.charAt(i) == t.charAt(j - 1)) {
                              dp[j] = Math.min(LIMIT, dp[j] + dp[j - 1]);
                          }
                      }
                  }
                  if (dp[t.length()] > Integer.MAX_VALUE) throw new ArithmeticException("result exceeds official 32-bit contract");
                  return (int) dp[t.length()];
              }
          }
          JAVA

          cat > "$out/DistinctSubsequences115Test.java" <<'JAVA'
          public final class DistinctSubsequences115Test {
              public static void main(String[] args) {
                  eq(3, DistinctSubsequences115.numDistinct("rabbbit", "rabbit"), "official rabbit");
                  eq(5, DistinctSubsequences115.numDistinct("babgbag", "bag"), "official bag");
                  eq(0, DistinctSubsequences115.numDistinct("abc", "abcd"), "target longer");
                  eq(1, DistinctSubsequences115.numDistinct("abc", "abc"), "equal strings");
                  eq(10, DistinctSubsequences115.numDistinct("aaaaa", "aa"), "equal chars choose positions");
                  eq(1, DistinctSubsequences115.numDistinct("abc", ""), "generalized empty target");
                  eq(0, DistinctSubsequences115.numDistinct("", "a"), "generalized empty source");
                  eq(0, DistinctSubsequences115.numDistinct("a".repeat(1000), "a".repeat(20) + "b"), "huge intermediate prefix with zero final");
                  exhaustiveBinaryOracle();
                  throwsArithmetic(() -> DistinctSubsequences115.numDistinct("a".repeat(50), "a".repeat(25)));
                  throwsIAE(() -> DistinctSubsequences115.numDistinct(null, "a"));
                  System.out.println("PASS official=2 combinatorial=yes empty-extension=yes saturation-prefix=yes exhaustive-binary=yes overflow-contract=yes invalid=yes");
              }

              private static void exhaustiveBinaryOracle() {
                  for (int n = 0; n <= 8; n++) {
                      for (int sm = 0; sm < (1 << n); sm++) {
                          String s = binaryString(n, sm);
                          for (int m = 0; m <= Math.min(5, n + 1); m++) {
                              for (int tm = 0; tm < (1 << m); tm++) {
                                  String t = binaryString(m, tm);
                                  int expected = brute(s, t, 0, 0);
                                  int actual = DistinctSubsequences115.numDistinct(s, t);
                                  if (expected != actual) throw new AssertionError("oracle s=" + s + " t=" + t + " expected=" + expected + " actual=" + actual);
                              }
                          }
                      }
                  }
              }

              private static int brute(String s, String t, int i, int j) {
                  if (j == t.length()) return 1;
                  if (s.length() - i < t.length() - j) return 0;
                  if (i == s.length()) return 0;
                  int count = brute(s, t, i + 1, j);
                  if (s.charAt(i) == t.charAt(j)) count += brute(s, t, i + 1, j + 1);
                  return count;
              }

              private static String binaryString(int len, int mask) {
                  StringBuilder b = new StringBuilder(len);
                  for (int i = 0; i < len; i++) b.append(((mask >>> i) & 1) == 0 ? 'a' : 'b');
                  return b.toString();
              }

              private static void eq(int expected, int actual, String label) {
                  if (expected != actual) throw new AssertionError(label + " expected=" + expected + " actual=" + actual);
              }
              private static void throwsArithmetic(Runnable r) { try { r.run(); throw new AssertionError("expected ArithmeticException"); } catch (ArithmeticException expected) {} }
              private static void throwsIAE(Runnable r) { try { r.run(); throw new AssertionError("expected IllegalArgumentException"); } catch (IllegalArgumentException expected) {} }
          }
          JAVA

          cat > "$out/source_boundary.json" <<'JSON'
          {
            "schema_version":"answer_source_boundary.v1",
            "canonical_id":"cq_q_6c3e7f9d826622bce0c25df5beee24a0",
            "checked_at":"2026-08-25",
            "repository_sources":[
              {"path":"note_tagged/6661b50a0000000006006ab3.json","question_id":"6c3e7f9d826622bce0c25df5beee24a0","preserved_text":"算法：不同的子序列 (LeetCode 115 - DP)"},
              {"path":"note_structured/6661b50a0000000006006ab3.json","preserved_text":"算法：不同的子序列 (LeetCode 115 - DP)"}
            ],
            "primary_problem":{"title":"LeetCode 115. Distinct Subsequences","locator":"https://leetcode.com/problems/distinct-subsequences/description/","verified_contract":"Given strings s and t, count distinct subsequences of s equal to t; generated test answers fit a signed 32-bit integer; lengths are 1..1000 and inputs contain English letters."},
            "preserved_constraints":["LeetCode problem number 115","Distinct Subsequences problem identity","dynamic programming requested by source"],
            "not_preserved":["implementation language","method signature in interview","whether interviewer expects 2D or 1D DP explanation","whether empty-string generalization should be discussed"],
            "candidate_contract_assumptions":["Java implementation for executable validation","official LeetCode 115 semantic contract supplies the recoverable problem statement","one-dimensional DP with descending target index","saturating intermediate counts at Integer.MAX_VALUE+1 while preserving every official-valid final answer"]
          }
          JSON

          javac "$out/DistinctSubsequences115.java" "$out/DistinctSubsequences115Test.java"
          stdout="$(java -cp "$out" DistinctSubsequences115Test)"
          test "$stdout" = 'PASS official=2 combinatorial=yes empty-extension=yes saturation-prefix=yes exhaustive-binary=yes overflow-contract=yes invalid=yes'
          STDOUT="$stdout" node <<'NODE'
          const fs=require('fs'),cid=process.env.CANONICAL_ID,qid=process.env.SOURCE_QID,out=`review/content_build/answer_batch_0030/${process.env.CANONICAL_ID}`;
          fs.writeFileSync(`${out}/code_validation.json`,JSON.stringify({schema_version:'answer_code_validation.v1',canonical_id:cid,result:'pass',validated_at:'2026-08-25',command:'javac DistinctSubsequences115.java DistinctSubsequences115Test.java && java DistinctSubsequences115Test',stdout:process.env.STDOUT,checks:['official examples rabbbit/rabbit=3 and babgbag/bag=5','equal-character positional combinations aaaaa/aa=10','target-longer and equal-string boundaries','generalized empty-string DP bases','1000-a source with enormous a^20 prefix count but final target ending b equals 0 under saturation','exhaustive binary-string cross-check against recursive oracle for source length <=8 and target length <=5','defensive rejection when final result exceeds official 32-bit promise','null rejection']},null,2)+'\n');
          fs.writeFileSync(`${out}/writer_research.json`,JSON.stringify({schema_version:'answer_writer_research.v1',canonical_id:cid,checked_at:'2026-08-25',review_state:'writer_complete_isolated_review_pending',sources:[{source_id:'repository-source',title:'Tagged and structured repository source preserving LeetCode 115 Distinct Subsequences DP identity',locator:'note_tagged/6661b50a0000000006006ab3.json',source_type:'repository_source_record',checked_at:'2026-08-25'},{source_id:'leetcode-115',title:'LeetCode 115 Distinct Subsequences official problem description',locator:'https://leetcode.com/problems/distinct-subsequences/description/',source_type:'primary_official_documentation',checked_at:'2026-08-25'},{source_id:'fixture',title:'Distinct Subsequences exhaustive bounded oracle and edge fixture',locator:`${out}/code_validation.json`,source_type:'executable_test_or_reproducible_experiment',checked_at:'2026-08-25'}],claims:[{claim_id:'source-boundary',text:'The repository source preserves the exact LeetCode 115 Distinct Subsequences identity and explicitly marks DP, so the official problem contract is recoverable while implementation language and interview API are not.',source_ids:['repository-source','leetcode-115'],answer_locations:['核心结论','1 分钟版','关键细节']},{claim_id:'problem-contract',text:'Official LeetCode 115 asks for the count of subsequences of s equal to t and promises the generated final answer fits a signed 32-bit integer.',source_ids:['leetcode-115'],answer_locations:['核心结论','1 分钟版','3 分钟版']},{claim_id:'implementation-validation',text:'The executable fixture verifies official examples, positional combinatorics, DP boundaries, saturation against enormous irrelevant intermediate prefix counts, exhaustive bounded binary cases against an independent recursive oracle, and defensive overflow handling outside the official final-answer promise.',source_ids:['fixture'],answer_locations:['3 分钟版','关键细节','原理机制','常见追问']}],source_question_coverage:[{question_id:qid,covered:true,answer_locations:['核心结论','1 分钟版','3 分钟版','关键细节','原理机制','常见追问']}],promotion_blocker:'isolated_independent_review_not_yet_performed'},null,2)+'\n');
          const task='tasks/answer-batches/TASK-20260711-0313-answer-batch-0030.md';
          let text=fs.readFileSync(task,'utf8');
          const note='- [ ] `cq_q_6c3e7f9d826622bce0c25df5beee24a0` writer stage complete: repository identity resolves to official LeetCode 115, the candidate derives and explains the one-dimensional descending-index DP, intermediate-count saturation avoids an unproven overflow assumption, and executable validation covers both official examples plus exhaustive bounded binary-string oracle comparison. Promotion remains blocked on isolated independent review and downstream evidence/strict gates.';
          if(!text.includes(note)) fs.writeFileSync(task,(text.endsWith('\n')?text:text+'\n')+note+'\n');
          NODE

      - name: Run repository gates for writer stage
        shell: bash
        run: |
          set -euo pipefail
          node scripts/content/check_question_coverage.js --check
          node scripts/xhs.js canonical check --noWrite
          node scripts/xhs.js review integrity --noWrite
          node scripts/xhs.js answer type-audit --check --noWrite
          node scripts/xhs.js answer validate --strict --noWrite
          node scripts/xhs.js validate all --noWrite
          npm test
          npm run ci:check
          npm run ci:answer:semantic
          npm run ci:answer:coverage

      - name: Commit candidate only from stable content head
        shell: bash
        run: |
          set -euo pipefail
          git fetch --depth=1 origin "$CONTENT_BRANCH"
          remote_head="$(git rev-parse origin/$CONTENT_BRANCH)"
          base_head='${{ steps.base.outputs.head }}'
          if [ "$remote_head" != "$base_head" ]; then
            echo "Content branch advanced during LeetCode 115 candidate build: $base_head -> $remote_head; fail closed and retry later."
            exit 0
          fi
          git add "review/candidates/answers/$CANONICAL_ID.md" "review/content_build/answer_batch_0030/$CANONICAL_ID" tasks/answer-batches/TASK-20260711-0313-answer-batch-0030.md
          git diff --cached --check
          git config user.name 'xhs-content-builder'
          git config user.email '43640935+liqiangcc@users.noreply.github.com'
          git commit -m 'content: stage batch 0030 distinct-subsequences candidate'
          git push origin "HEAD:$CONTENT_BRANCH"
